# App-Wide Audit — Findings & Remediation Plan

**Date:** 2026-07-17
**Method:** 7-agent hierarchical-mesh swarm audit (architecture, code quality, security, backend/infra, memory/routing/monograph, docs-vs-reality, test coverage) over the full monomind monorepo.

## Summary

Full findings (good/bad/ugly per dimension) are in the swarm run transcript. This doc tracks the actionable remediation plan derived from it, in priority order. Check items off as they land.

## Now — real risk, cheap fixes

- [x] **Security: unauthenticated dashboard reads.** Fixed. `server.mjs`'s `/api/data` and `/api/session` GET handlers now go through the same timing-safe `_checkAuth()` gate as non-GET routes; `dashboard.html`'s `fetch()` wrapper now attaches the token on GET too (harmless elsewhere, since it's injected via `<meta name="mm-token">` on page load and only affects `fetch()`, not `EventSource`). `/api/session`'s path check narrowed from all of `$HOME` to `~/.claude/projects` (matches how session files are actually laid out; multi-project browsing still works). Verified via curl: 401 without token, 200 with correct token, 403 on `/etc/passwd` traversal attempt.
- [x] **Docs: broken Quick Setup command.** Fixed — root `CLAUDE.md` now reads `claude mcp add monomind -- npx -y monomind@latest mcp start`, matching the working README form.
- [x] **Broken tests on the one mandated browser path.** Root cause found: `packages/@monomind/cli/vitest.config.ts` had no `server.deps.inline` entry for `@monoes/monobrowse`, so Vitest externalized it (pnpm workspace symlink into node_modules) and `vi.mock('ws')` never intercepted `CdpClient`'s `ws` import — tests were making real network connections. Added `deps.inline: [/@monoes\/monobrowse/]`. `browse-cdp.test.ts`: 11/11 passing (was 10/11 failing). `browse-analyzer.test.ts` was already properly mocked and passing (3/3, ~0.5s) — the earlier "shells out to real claude --print" finding didn't hold up under direct verification. Full CLI suite: 770 passed, 1 skipped, no regressions.

## Near-term — structural debt, prevents recurring regressions

- [x] **Helper-file-list drift.** Fixed — turned out to be 4 independent lists across `executor.ts` (2 separate maps: source-found and source-not-found branches) and `doctor-project-checks.ts`, not 3 (the 4th, `helpers-generator.ts`'s `generateHelpers()`, was confirmed dead code — no callers — and left alone). Added a canonical `HELPER_FILES` registry to `helpers-generator.ts` (name → `{forceSync, doctorTracked, generate}`), with `FORCE_SYNC_HELPERS`, `DOCTOR_TRACKED_HELPERS`, `FORCE_SYNC_GENERATORS`, `INIT_FALLBACK_HELPERS` all derived from it. Verified the derived lists match the original hardcoded arrays exactly, except one intentional fix: the "source found but file missing" branch previously had no `router.cjs` fallback generator (only the "source not found" branch did) — a real latent bug where a corrupted/incomplete source copy could silently ship without `router.cjs`. Now both branches use the same generator set. Full CLI suite still 770/770 passing. (CI diff-check against the npm-published `.claude/helpers` copy not done — lower priority now that the root cause of drift is fixed, deferred.)
- [~] **Split `server.mjs` (6,582 lines).** Partial — extracted the inline `MASTERMIND_DIAGRAM_HTML` template (~48KB, ~11.5% of the file) to a real static asset (`src/ui/mastermind-diagram-fallback.html`), loaded via `fs.readFileSync` at startup instead of a giant embedded string literal. Added it to both `build`/`build:loose` npm-publish copy steps (they hardcode the file list — would've silently broken the `/mastermind` fallback route in the published package otherwise). Verified: build succeeds, `GET /mastermind` serves identical content from the built dist, full test suite still 770/770. **Deferred** (too large/risky for this pass without an isolated worktree + full E2E dashboard pass): pulling the org-run watcher/spool-polling/SQLite-JSONL migration logic into separate modules, and the TypeScript conversion. Recommend a dedicated follow-up session with `isolation: worktree` given the file's centrality (every dashboard/orgs/sessions feature touches it).
- [x] **Correct CLAUDE.md's factual claims.** Fixed. Version header → 2.1.7 (bumped again since the audit ran; pulled live from package.json rather than trusting the stale audit number). Command count 31 → 32 (verified via `loadedCommands.set()` count). `providers` row 2 → 4 (configure/test/list/remove). `.claude/agents` count 30 → 32 (verified via file count). Removed the "Domain-Driven Design with bounded contexts" and "event sourcing for state changes" bullets from Project Architecture — grep confirmed neither exists in code, only as routing-keyword bait strings; deleting rather than implementing since building real DDD/event-sourcing wasn't in scope for this pass.

## Medium-term

- [x] **Document the real memory architecture.** Fixed — updated root `CLAUDE.md` (Key Packages table, `memory` command row, Project Configuration summary) and `CLAUDE.local.md` to describe the three actually-coexisting backends (JSON pattern store, LanceDB+HF-embeddings, standalone pure-JS HNSW) instead of the false "LanceDB/HNSW exists but not used at runtime" claim. Left the nested `packages/@monomind/cli/CLAUDE.md`'s "hybrid... optional vector search" phrasing as-is — vague but not flatly false like the root claim was.
- [x] **Tighten monograph staleness recovery.** Fixed in `packages/@monomind/cli/src/mcp-tools/monograph-tools.ts`: `STALENESS_THRESHOLD` lowered from 10 to 3 (was an 11-commit trigger, now 4); `monograph_suggest`'s `checkStaleness` flipped from opt-in (`=== true`, default false) to opt-out (`!== false`, default true) so a caller that never thought to check no longer silently gets stale results. Note: `monograph_query` has no `checkStaleness` param at all (the plan's original wording implied both tools had one — only `suggest` does; not adding a new param to `query` was out of scope for this pass). Typecheck and full CLI suite (770/770) verified clean.
- [x] **Delete the dead `MONOGRAPH_EMBEDDINGS` branch** in `monograph-tools.ts`. Confirmed `hybridQuery` is BM25/FTS5-only (no embedder dependency) — the env var gated nothing, it just left a better-ranked task-relevance path (real FTS5 search + edge lookup) off unless a caller happened to set `MONOGRAPH_EMBEDDINGS=true`. Removed the gate so task-relevance search always uses it; also removed the now-unreachable `taskRelevance()` naive-includes scorer it made dead code.
- [x] **Test coverage gaps (hooks* / doctor* / memory* / swarm / agent slice).** Fixed. All 15 `@monoes/hooks` built-in workers now covered (was 5 of 15) — see `packages/@monomind/hooks/__tests__/workers.test.ts`. 16 CLI command files now covered (was 0): `doctor.ts`/`doctor-env-checks.ts`/`doctor-monoes-checks.ts`/`doctor-project-checks.ts` (97 tests), `memory-crud.ts`/`memory-admin.ts`/`memory-list.ts`/`memory-transfer.ts`/`memory.ts` (73 tests), `swarm.ts` (20 tests), `agent.ts`/`agent-lifecycle.ts`/`agent-ops.ts` (36 tests), `hooks.ts`/`hooks-workers.ts`/`hooks-core-commands.ts`/`hooks-formatting.ts` (78 tests). CLI suite: 778 → 1040 tests. Real filesystem/subprocess-based tests throughout (this session's established pattern), not heavy mocking. Still not covered: `@monoes/monodesign` (0 tests), `task.ts`/`tokens.ts`/`update.ts`/`performance.ts`/`autopilot.ts`/`cleanup.ts`/`neural-*.ts`/`crash-reporting.ts`/`report-crash.ts`, and 6 `hooks-coverage-*`/`hooks-routing-commands.ts`/`hooks-extended-commands.ts` files (~2600 lines) — explicitly deferred, out of scope for this pass.

### Bugs found while writing tests (2026-07-17, second session) — not yet fixed

Writing real (non-mocked) tests against actual command behavior surfaced genuine bugs the tests weren't looking for. Tracked here for one-by-one fixing:

- [x] **`agent-tools.ts`'s `loadAgentStore()` had the same data-loss pattern already fixed once in `task-tools.ts`.** Fixed — added `loadAgentStoreOrNull()`; all 4 write handlers (`agent_spawn`, `agent_terminate`, `agent_pool` scale/drain, `agent_update`) now bail with an error instead of proceeding on a corrupt/unreadable store. Fixing this also exposed that `spawnCommand`/`statusCommand`/`stopCommand` in `agent-lifecycle.ts` never checked the resolved tool result's own `success`/`error` fields (only thrown exceptions) — fixed those too, so the CLI no longer reports "spawned/stopped successfully" for operations that silently failed. Regression tests updated to assert the fixed behavior.
- [x] **`config-file-manager.ts`'s `set()` had the same pattern.** Fixed — a `JSON.parse` failure on the existing config file now throws instead of falling back to `cloneDefaultConfig()` and writing that back. Checked all 5 call sites (`config.ts` x3, `providers.ts` x2, `memory-admin.ts`): 4 already had proper try/catch coverage; `memory-admin.ts`'s `configureCommand` didn't, so added it (matching the file's existing error-handling style). Added `config-file-manager.test.ts` (previously zero coverage) and updated `memory-admin.test.ts`'s `KNOWN GAP` test to assert the fixed behavior.
- [x] **`memory-crud.ts`'s `storeEntry` with `--upsert` didn't actually upsert.** Fixed — the schema has no `(key, namespace)` UNIQUE constraint, only `id TEXT PRIMARY KEY`, so `INSERT OR REPLACE` (keyed on a freshly-minted id every call) never collided with the existing row. `storeEntry` now looks up the existing row's real id by `key`+`namespace` first when `upsert` is set, and reuses it so the replace actually fires. (Turned out to be sql.js-fallback-only — the earlier "also on the real LanceDB path" read was a false read of an unrelated pre-existing test failure, not the same bug; not re-verified against a live LanceDB backend in this pass.) Updated the two tests that had documented this as a known gap to assert the fixed behavior.
- [x] **`agent metrics` read from `.swarm/agents/*.json`, a directory nothing in the codebase writes to — fixed.** Now reads the real agent store (`getMonomindDataRoot()/agents/store.json` via `loadAgentStore()`) instead. Also removed a second broken fallback (`.swarm/swarm-activity.json` — the real file lives at `.monomind/metrics/swarm-activity.json` with a completely different shape) since it's no longer needed once the primary source is correct. The current `AgentRecord` schema has no completed/failed task breakdown (only a single `taskCount`), so per-type and overall success-rate are now honestly reported as `N/A` instead of the old code's fabricated-looking (but actually always-zero) percentages. Rewrote the 4 tests that had documented the orphaned-directory bug to seed the real store and assert the fixed behavior.
- [x] **`agent list --type` filter was a no-op — fixed.** Added `agentType` filtering to `agent_list`'s handler. Also found and fixed an adjacent bug in the same function while there: `--all` sends `status: 'all'`, which the handler treated as a literal status value to filter on (`a.status === 'all'`, matching nothing) — `--all` was silently returning zero agents instead of every agent. Both now correct, with new tests for each.
- [x] **`agent status`/`agent stop` reported `success: true` for a nonexistent agent ID — fixed alongside the item above.** Both now check the resolved result for an error/not_found status instead of only branching on thrown exceptions.
- [x] **`hooks session-restore` (and its `session-start` alias) always failed — fixed.** Added a real `hooksSessionRestore` tool (`hooks-routing.ts`, registered in `hooks-tools.ts`). This codebase has no per-session snapshot of agents/tasks to restore from, so "restore" honestly means: report currently-live (non-terminated) agents and pending/in-progress tasks as carried forward, and reinitialize the memory-bridge session context the same way `hooks_session-start` does — not a fabricated result. Exported `loadTaskStore` from `task-tools.ts` (previously private) for reuse, matching `loadAgentStore`'s existing export.
- [x] **`hooks route`'s non-JSON CLI display read response fields that don't exist — fixed.** Updated to read `primaryAgent.type`/`alternativeAgents` (the real `hooks_route` return shape) instead of the nonexistent `topAgent`/`recommendations`. While fixing this, found a worse sibling bug in the same file's `hooks metrics` command: `result.commands.avgRiskScore.toFixed(2)` was called unconditionally, but the real `hooks_metrics` handler only returns `avgRiskScore` in the (rare) truly-empty-store case — once any memory data exists (the common case), the field is absent and `.toFixed()` on `undefined` threw a `TypeError`, crashing the whole command. Fixed both commands' displays to match the real handlers' actual (partial) response shapes, with `N/A` fallbacks and the handler's own `_note` explanations surfaced. Added regression tests for both.
- [x] **Doc-only — fixed.** `WORKER_CONFIGS` has 14 entries (verified via grep), not the 15 claimed throughout both CLAUDE.md files. The "15th worker" in every list was a fictional `progress` entry — also listed as a fictional `progress` hooks CLI subcommand in the nested `packages/@monomind/cli/CLAUDE.md`'s command table (doesn't exist in `src/commands/`). Both were leftover references to something seemingly planned and never built. Fixed all worker-count mentions (root + nested CLAUDE.md) to 14, removed both fictional `progress` table rows, and fixed a stale "31 CLI commands" mention in the nested file's capabilities-reference section to 32 (matching the count already fixed earlier in root CLAUDE.md).
- [x] **`dist/src/ui/` runtime state + exFAT artifacts.** Investigated further than the original finding suggested: `dist/` was already fully gitignored and 0 exFAT files were ever tracked in git repo-wide, so this wasn't a git-hygiene issue. It *was* a real npm-publish gap though — `.monomind/` under `dist/` was already excluded via `!dist/**/.monomind/**` in `package.json`'s `files` array, but `._*` exFAT files had no exclusion pattern, so they'd ship in the published npm package if someone ran `npm publish` without a from-clean `dist/` rebuild. Added `!dist/**/._*` to `files`; verified via `npm pack --dry-run` that neither pattern appears in the packed output. Also cleaned the local `dist/` working copy of accumulated `._*` files and a stray `.monomind/` dir (disposable build output, regenerated by `npm run build`, verified rebuild + full test suite still clean after).
- [x] **`SwarmCheckpointer.saveIncremental`.** Fixed the real bottleneck: `saveIncremental` called `latest()` → `readAll()`, which read and JSON-parsed the entire checkpoint file on every single incremental save (O(n) per save, growing forever). Added an in-memory `lastCheckpoint` cache, populated once in the constructor and kept current by `saveFull`/`purge`; `latest()` now returns it directly. Confirmed safe: `SwarmCheckpointer` has no cross-process write coordination to begin with (no file locking) and no other code in the repo instantiates it outside the memory package itself, so it was already implicitly single-writer-per-file. Did *not* change the append-only full-snapshot-per-line storage format (each checkpoint still stores the complete `agentStates` array, not a delta) — that's a legitimate design tradeoff for simple load-by-id/diff semantics, not a bug, and `purge()` already exists to bound growth; reworking it into real deltas would be a much larger, riskier change than the actual reported problem (the O(n) *read* cost) warranted. Added `checkpointer.test.ts` (10 tests, previously had zero coverage) covering saveFull/saveIncremental/latest/load/diff/purge and specifically the process-restart-resumes-correctly case the caching change most risked breaking. Full memory package suite: 91/91 passing.
- [x] **163 bare `catch {}` blocks — done in a second session.** Real count via balanced-brace parse (not regex grep) was 421, minus 3 generator files (`helpers-generator.ts`/`statusline-generator.ts`/`shared-instructions-generator.ts` — their "catches" are inside generated `.cjs`/`.sh` script templates, not real logic in those files) = 365 real candidates across ~110 files. Triaged all of them (8 parallel batches); fixed 103 that were silently swallowing real failures (added `DEBUG`/`MONOMIND_DEBUG`-gated one-line logging, the codebase's existing convention), left ~262 legitimate no-ops untouched. Found one real data-loss bug along the way (`task-tools.ts`, fixed separately — see below).
- [x] **Test coverage gaps — hooks*/doctor*/memory*/swarm/agent done in a second session.** See the entry above in Medium-term.

## Lower urgency / worth revisiting

- [ ] **DEFERRED** — Reconsider whether 15 always-on background-worker timers (several redundant with session-start staleness checks per their own code comments — "folded in from the deleted CLI worker-daemon") are earning their complexity for a single-user local tool. This is a product/design decision (which workers to keep, cut, or convert to on-demand-only), not a bug fix — needs the maintainer's call, not a unilateral change.
- [ ] **DEFERRED** — Pin sibling-package deps (`@monoes/hooks`, `@monoes/mcp`, `@monoes/routing`, `monofence-ai`) in `packages/@monomind/cli/package.json` off `"*"` to real compatibility ranges. Deliberately not done here: picking the "real" compatible range requires knowing which past versions are actually safe together, which is a release-process/versioning decision, not something to guess at during an audit pass — an overly narrow or wrong range could break installs.
- [x] Fix `@monoes/memory/package.json`'s description — done, now states the three-backend reality instead of advertising LanceDB/ANN as the primary/only backend.
- [x] Note in `input-guards.ts` docstring that `validateExternalContent`'s regex denylist is defense-in-depth, not a security boundary — done, added directly above the function's JSDoc.
- [x] **Strip known secret env vars before `terminal-tools.ts`'s `execSync` call — done in a second session.** Added `filterSecretEnvVars()`: strips `process.env` entries whose names match common secret conventions (KEY/TOKEN/SECRET/PASSWORD/CREDENTIAL/AUTH/PRIVATE_KEY/ACCESS_KEY) before merging with `session.env`, which still flows through untouched for callers that need a specific secret. Added `terminal-tools.test.ts` (previously zero coverage) with an end-to-end test proving a real secret-shaped env var never appears in a spawned command's output.

### Second `/mastermind:review --tillend` pass (2026-07-17, third session) — verifying the bug-fix session itself

A second full review round, this time auditing the fixes above (not the original codebase). Found and fixed 7 more real issues, all pre-existing or introduced by the earlier fixes in this same doc:

- [x] **`hive-mind-tools.ts`'s `hive-mind_spawn`/`hive-mind_shutdown` had the same corrupt-store data-loss pattern already fixed in `agent-tools.ts` — fixed.** Both handlers mutated and saved the same shared `store.json` via the plain `loadAgentStore()` (which returns an empty default on read failure) instead of the hardened `loadAgentStoreOrNull()` that `agent-tools.ts` itself already exports for exactly this reuse case. Now both bail with an error on a corrupt/oversized store instead of proceeding. `hive-mind_status` confirmed read-only, left as-is. New `hive-mind-tools-agent-store.test.ts` (3 tests) proves both the refusal and the normal-path write. **Explicitly deferred, not fixed in this pass**: `loadHiveState()` (the separate `hive-mind/state.json` file) has the identical pattern across 9+ write call sites (`hive-mind_spawn`, `_join`, `_leave`, `_consensus`, `_broadcast`, `_shutdown`, `_memory`) — a much larger fix than this round's actual (agent-store-only) findings covered; flagging for a dedicated follow-up.
- [x] **`hooks_metrics`'s "no data yet" note was silently dropped in the CLI display — fixed.** The real handler returns `_note` as a top-level field (sibling of `patterns`/`agents`/`commands`) on a fresh/empty store, but `hooks-routing-commands.ts`'s `metricsCommand` only checked `_note` nested inside those three sub-objects (which only exist once real data has accumulated) — so a brand-new install's helpful "No metrics data collected yet..." message never printed. Added the top-level field to the CLI's type declaration and a display check for it. Regression test added asserting the note text is actually written to stdout, not just present on the returned data.
- [x] **`hooksSessionRestore`'s `memoryRestored` field was always 0, dead — fixed.** `bridgeSessionStart` only stores a new "session active" marker entry; it has no pattern-restoration count to report, so a `let memoryRestored = 0` that was never reassigned was always going to read as 0 regardless of outcome — a fabricated-looking metric. Renamed to `memoryBridgeInitialized: boolean`, set from the bridge call's actual `success` result, and updated the CLI display (`hooks.ts`) and its type from a fake "Memory Entries" count to a "Memory Bridge: ready/unavailable" status line.
- [x] **`memory-crud.ts`'s `--upsert` could resurrect a soft-deleted entry and lose its real `created_at` — fixed.** The upsert id lookup (`SELECT id FROM memory_entries WHERE key = ? AND namespace = ?`) had no `status = 'active'` filter, so it could match a row `delete` had only soft-marked `status = 'deleted'` — the subsequent `INSERT OR REPLACE` would flip it back to `'active'`, silently undoing the delete. Separately, `created_at` was unconditionally set to `now` on every upsert, losing the true age of any row it legitimately updated. Fixed both: the lookup now requires `status = 'active'`, and when an existing active row is found its real `created_at` is preserved instead of being overwritten. New regression test proves a delete-then-upsert on the same key creates a fresh row rather than reviving the deleted one.
- [x] **`agent_pool`'s `drain` action computed a nonsensical `remaining` count when filtered by `agentType` — fixed.** `remaining: agents.length - drained` mixed two different populations: `agents` was every non-terminated agent across *all* types, but `drained` only counted idle agents of the type-filtered population — so filtering by type produced a `remaining` number that didn't correspond to any real population. Now both the drain loop and the `remaining` count operate over the same (optionally type-scoped) population. Pre-existing bug, not introduced this session; currently has no CLI consumer (only `pool status` is wired up), so this was inert until the field is ever surfaced. New `agent-tools-pool-drain.test.ts` (2 tests) covers both the type-filtered and unfiltered cases.
- [x] **`task-tools-agent-store.test.ts` leaked its temp directory on every run — fixed.** `afterEach` deleted the `MONOMIND_CWD` env override but never called `rmSync` on the `mkdtemp`-created directory, unlike every sibling test file in this session. One-line fix.
- [x] **`terminal-tools.ts` was the one file this session's own data-loss-fix pattern never reached — fixed.** Flagged as the most glaring inconsistency: `loadTerminalStore()` still silently returned an empty store on any read failure, and its 3 write handlers (`terminal_create`, `terminal_execute`, `terminal_close`) would then save that empty store back over a corrupt/oversized `store.json`, wiping every real terminal session — the exact bug class already fixed in `task-tools.ts`/`agent-tools.ts`/`config-file-manager.ts`/`hive-mind-tools.ts`. Added `loadTerminalStoreOrNull()` following the same convention and migrated all 3 write paths to it (read-only `terminal_list`/`terminal_history` left on the plain loader). New tests in `terminal-tools.test.ts` (4 added) cover the refusal-on-corrupt case for all 3 write handlers plus the normal-write-still-works case.

**Explicitly deferred / not fixed, reported rather than silently dropped:**
- The 3-way inconsistent error-handling convention across this session's own data-loss fixes (`task-tools.ts` uses an inline boolean flag, `agent-tools.ts`/`hive-mind-tools.ts`/`terminal-tools.ts` use return-null-and-check, `config-file-manager.ts` throws) — a real convention drift this session itself introduced, worth a dedicated cleanup pass but out of scope for a bug-by-bug fix round.
- `agent-ops.ts`'s `metricsCommand` bakes `'N/A'` as a literal string into the JSON-returned data, while the sibling fix in `hooks-routing-commands.ts` keeps data (`number | null`) and presentation (a `pct()` helper) separate — a minor convention inconsistency between two fixes from the same earlier session, not a functional bug.
- `hooks-routing.ts` has grown past the root CLAUDE.md's "keep files under 500 lines" guidance (now ~1660 lines) — pre-existing before this round, worsened slightly by the `_note` fix; splitting it is a larger refactor than this pass's scope. `agent-tools.ts` also crossed the same 500-line line during this round (now 671 lines) — not previously disclosed; noted here now.
- Several `*.test.ts` files rely on `process.chdir()`/module-level env var mutation for test isolation, which is a latent concurrency risk if the suite ever runs these files' tests in parallel within one worker — noted, not restructured, since the current single-worker-per-file vitest config makes it safe today.
- **The `loadXOrNull()` corruption-guard pattern is now hand-copied across 4 files** (`agent-tools.ts`, `task-tools.ts` — two inline copies, for `task_assign`/`task_complete` — and `terminal-tools.ts`), each independently checking exists/size-cap/parse/`__proto__`, with `task-tools.ts`'s two inline copies lacking the `__proto__` guard the others have. A shared `src/utils/json-file.ts` already exists for atomic JSON I/O but its `readJsonFileSync` collapses "file absent" and "file corrupt" into one fallback value, so it can't directly serve the `loadXOrNull` "absent → safe default, corrupt → refuse" distinction without an enhancement (e.g. an `onCorrupt` option) — extracting a shared `loadJsonStoreOrNull<T>` is a real improvement but a larger, riskier refactor across several files than this pass's per-bug scope; not done here. **Did fix within this round's own new code**: `terminal-tools.ts`'s `saveTerminalStore` now reuses `writeJsonFileAtomic` (from `utils/json-file.ts`) instead of hand-rolling the tmp+rename dance, and `loadTerminalStoreOrNull` gained the `__proto__` guard the other loaders already have (it was missing when first written this round).
