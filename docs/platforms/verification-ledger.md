# Platform verification ledger

This generated ledger records the upstream schema or runtime evidence required before a platform capability can be declared `native`.

No capabilities are currently promoted: the canonical registry conservatively reports target-native cells as `experimental` until a renderer and cited evidence are added together.
