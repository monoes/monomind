{
  "name": "@monoes/memory",
  "version": "1.0.2",
  "type": "module",
  "description": "Memory module - LanceDB backend, ANN indexing, vector search, hybrid SQLite+LanceDB backend (ADR-009)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*.js"
  },
  "scripts": {
    "test": "vitest run",
    "bench": "vitest bench",
    "build": "tsc",
    "prebuild": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "npm run build && node --input-type=module --eval \"import('./dist/index.js').then(m => { const required = ['ControllerRegistry','HnswLite','PersistentSonaCoordinator','RvfBackend','RvfLearningStore','RvfMigrator']; const missing = required.filter(k => !m[k]); if (missing.length) { console.error('Missing exports:', missing); process.exit(1); } console.log('All required exports present'); });\""
  },
  "dependencies": {
    "sql.js": "^1.10.3"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.0.0",
    "@lancedb/lancedb": "^0.30.0",
    "apache-arrow": ">=15.0.0 <=18.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/sql.js": "^1.4.9",
    "vitest": "^4.1.4"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public",
    "tag": "alpha"
  },
  "os": [
    "darwin",
    "linux",
    "win32"
  ],
  "cpu": [
    "x64",
    "arm64"
  ]
}
