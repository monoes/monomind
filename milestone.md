# Monomind — Project Milestones

A full-monorepo review, package by package: what's actually shipped ("Achieved Milestones," verified against the real source/docs/tests) and what's worth doing next ("Future Milestones," each carrying an impact score). This is an ideation stepping stone, not a committed roadmap — add, re-score, and prune freely.

**Revision 9 — execution begins (2026-09-16, 16:30–23:50 CEST).** Closed with a self-review of everything above: a full line-by-line diff read found the settings/helpers-flag divergence between `init hooks`/`init skills` presets was safe (verified no preset actually combines `helpers: true` with `settings: false`), a comment that overstated what `writeSettings` does (fixed), and two real bugs the earlier work had missed — `tests.yml`'s new `Typecheck` step ran before the dependency build it needs (`@monoes/monobrowse` self-imports itself; would have failed "Cannot find module" on a fresh checkout — fixed and verified by clearing every package's `dist/` and rerunning the corrected order), and `close-browser.test.ts`'s 20 s timeout had now failed three separate full-verify runs this session under host load (raised to 60 s). A subsequent `pnpm verify` completed with zero failures. This revision marks the first items actually implemented rather than reviewed. A real `pnpm verify` gate (build + typecheck + lint + every package's tests, chained into one command; wired into CI) replaced the broken root `build` script. Four bug fixes landed through it: `capture-handler.cjs`'s `.unref()` (subagent-spawn stall), the SessionStart hook's lazy global-npm resolution, `monomind status`'s System Resources crash (an unbound method reference thrown from inside a swallowing catch), and monobrowse's `openUrl()` now failing loudly on a refused connection instead of reporting success against Chrome's error page. Separately, `graphify-freshen.cjs` — and every other internal use of "graphify" (a distinct external project's name, not this product's) as if it were this product's own naming — was renamed to `monograph-freshen.cjs`/`monograph`, matching the `@monoes/monograph` branding used everywhere else. A follow-up pass then went further at the user's explicit direction (no need to preserve back-compat, since `init --force` already rebuilds `monograph.db` unconditionally): the 14 deprecated `graphify_*` MCP tool shims are deleted outright rather than kept for compatibility, and `doctor`/`init --force` gained a real migration path for any project still on the old naming — a check that warns and points at `init --force`, which now deletes the obsolete hook file *and* strips its now-dead command from `settings.json` before the existing hook-merge runs (without that second part, the merge's own "preserve anything unknown" design would have kept the old command as a duplicate hook pointing at a file that had just been deleted). All landed through the same worktree → implement → `pnpm verify` → merge cycle that subsequent items will keep following; see each section's Achieved Milestones for what moved. **Closing full-session review:** an independent Code Reviewer and a Security Engineer, run in parallel over all 13 commits, both came back clean — no bugs, no security findings above informational, every claim (the build-gate fix, the CI-ordering fix, the obsolete-helper deletion's blast radius) independently re-derived and reproduced rather than trusted from commit messages. Only fixable gap found: this section's own version label, stale against a concurrent release (see the version note below).

**Revision 8 — every lens accounted for (2026-09-16, 06:55 CEST).** Eight revisions, ~45 agents, five review rounds. Revision 8 adds the last outstanding report, performance round 2: the SessionStart chain measured hook by hook (≈ 690 ms serialized, 523 ms of it `session-restore`, whose CPU profile is 80 % three avoidable pieces), the dashboard's startup, memory, and per-route cost (uncached token routes at 142 ms CPU and ~+200 MB RSS per page view; four live dashboards ≈ 1 GB RSS on one machine), org bus/forwarder costs, and the MCP server's RSS curve. No lens is now missing. Revision 6 applied a whole-document calibration pass (scores made consistent across sections; 16 duplicate groups merged into canonical homes; weak-evidence items demoted), added round 4's findings (the build script that doesn't build the product, `init` re-prefixing its own output, four reproduced at-rest/federation security defects), and corrected the former headline item's reach. Revision 7 adds the four lens reports that arrived after the loop closed: a monobrowse/monodesign end-to-end run (every `browse` command idles 5 s; `console`/`errors`/`har` are always empty from the CLI), a README promise-by-promise audit (the Quickstart's first command, `/mastermind:autodev`, does not exist; the Second Brain's headline sentence is disabled by the shipped default), an 8-task evaluation of the graph against grep (2 wins, 1 tie, 5 losses, 2 confident wrong answers; `monograph_suggest task=` is dead for code tasks), and a cross-platform static survey (18 background spawns without `windowsHide`; the control server can never be stopped on Windows). One caveat: rounds 1–3's *runtime* measurements through `node bin/cli.js` ran against a CLI `dist` built on 2026-09-09, because the root `pnpm build` never builds the CLI package (see umbrella); every file:line claim was made against current source.

## How to read the scores

Every future item starts with `[N/10]` — **impact on users and on the project's core promise**, not effort. `(quick win)` marks items that look like under a day of work.

| Score | Meaning |
|---|---|
| **10** | Fixes something currently broken or unsafe for most users, or unlocks the core promise |
| **8–9** | Major capability, reliability, or trust gain for a large share of users |
| **6–7** | Solid improvement with moderate reach, or high value at low effort |
| **4–5** | Nice-to-have, niche audience, or hygiene with some user-visible benefit |
| **1–3** | Cosmetic / internal tidiness, low user impact |
| **0** | Not worth doing or unsupported by evidence → removed |

## Top 10 for a real user of `npx monomind init` + Claude Code

1. **Hook enrichment never reaches the model** — the graph-first promise is undelivered in every session (`MONOMIND_HOOK_QUIET=1`, plain stdout on tool hooks).
2. **better-sqlite3 ABI failures and the EOL Node floor** — when install fails, nothing works (#231 and four siblings); the floor and the ABI story are one decision.
3. **The dead hardened MCP loop** — on the loop `.mcp.json` actually runs, every tool result is double-wrapped, `isError` is lost, payloads are 9–27% larger.
4. **Phantom references, the generated CLAUDE.md, and the README's retired commands** — 42 nonexistent tools, 4 nonexistent CLI groups, 38 phantom routing agents, `init --wizard`, "MUST init monoswarm", and a Quickstart whose first command (`/mastermind:autodev`) has no file: the model and the user burn turns on instructions that can't work.
5. **Everything is persisted verbatim, and it's committable** — the default hooks write every Bash command, Write body, and prompt to disk unredacted at 0644, and init's generated `.gitignore` leaves those stores (plus the dashboard token and org scrollback) committable.
6. **Crash reporter files public GitHub issues under the user's identity, by default.**
7. **Cost understated ~3×** — the number every user reads is wrong and says nothing about it.
8. **`config_set` silently deletes provider API keys** — data loss from a default-exposed tool.
9. **Unknown subcommands and flags exit 0** — agents "succeed" doing nothing; scripts can't trust exit codes.
10. **`@monoes/memory` fails under the concurrency a normal session already produces**, and swallows the failures.

## First four PRs (sixteen quick wins, grouped by file locality)

**PR 1 — Hooks that help and tell the truth** (`.claude/helpers/handlers/capture-handler.cjs` ✅ done 2026-09-16, `helpers/utils/monograph.cjs`, `helpers/statusline.cjs`, `helpers/handlers/route-handler.cjs`, `helpers/event-logger.cjs`, `src/init/settings-generator.ts`, `src/init/write-runtime-config.ts`): drop `MONOMIND_HOOK_QUIET` and emit `hookSpecificOutput.additionalContext` from tool hooks; statusline counts only real graph calls and reads Claude Code's stdin for model/cost; redact tool inputs in the event log and drop the `raw` field; generate the same `.monomind/.gitignore` the repo itself uses. *Effect:* subagent hooks stop stalling, graph hints reach the model, the statusline shows the real model and cost, secrets stop landing in committable logs.

**PR 2 — One MCP loop, honest catalog, working dashboard** (`bin/cli.js`, `bin/mcp-server.js`, `src/mcp-server.ts`, `mcp-tools/config-tools.ts`, `mcp-tools/guidance-tools.ts`, `mcp-tools/system-tools.ts`, `ui/dashboard.html`, `ui/collector.mjs`, `ui/routes-monoes.mjs`): export `handleMCPMessage` and have both bins delegate; `config_set` through `configManager`; `guidance_*` derived from the live registry; one `getPackageVersion`; `Object.assign(window, {…})` for the 129 `_`-prefixed handlers + the `on*="name("` test; `message.id` dedup + `subagents/` walk; stop writing the OAuth token into `.mcp.json`. *Effect:* every MCP result correctly shaped and smaller, no API-key loss, no phantom routing, dashboard buttons and token counts work, no credentials in a shared file.

**PR 3 — The CLI means what it says** (`src/commands/index.ts`, `parser.ts`, `suggest.ts`, `pricing/model-pricing.ts` + its three copies, `services/crash-reporter.ts`, root `package.json` ✅ build/lint/typecheck/verify done 2026-09-16, `packages/@monomind/memory/src/sql-driver.ts`, `memory-bridge.ts`, `orgrt/remote.ts`): reject unknown subcommands/flags (exit 2, suggestion on stderr); one pricing table with fable-5/5.1 rows and an `unpriced` marker; crash reporter prompts once on a TTY; `tx.immediate()` + bounded `SQLITE_BUSY` retry; stopword-free coverage gate; SSH payload on stdin. *Effect:* typos fail loudly, cost numbers stop being 3× low, nothing is filed to GitHub without consent, memory writes stop failing, federated hosts can't be commanded by a prompt-injected role.

**PR 4 — Browse, graph answers, and the README** (`bin/cli.js`, `commands/browse.ts`, `mcp-tools/monograph/query-tools.ts`, `impact-tools.ts`, `README.md`, `scripts/`): dispose the CDP client (or exit) after a browse subcommand + a `browse get title < 500 ms` test; `open` exits 1 on `chrome-error://`; `monograph_suggest task=` derives questions from the hits when no inferred edge touches them; non-test callers first and tests collapsed to a count in `context`/`impact`/`neighbors`; retarget the six retired `/mastermind:*` names in the README and add a CI test that every `/mastermind:<x>` in README/doc resolves to a command file. *Effect:* a 20-step browser test stops burning ~100 s idle, the tool CLAUDE.md mandates before every multi-file task returns something, callers lists stop reading "only tests call this", and the Quickstart's first command exists.

## Top priorities (every item scored ≥ 8 after calibration; 7s are listed in their sections)

| Impact | Item | Section |
|---|---|---|
| **9** | Almost no hook enrichment reaches the model: shipped `MONOMIND_HOOK_QUIET=1` zeroes per-prompt output, tool-hook hints go to plain stdout, and the statusline's "📊75% graph · 💰$2.15 saved" counts those unseen hints | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **9** | Native-module (better-sqlite3 ABI) install resilience and the Node floor: open #231 blocks the graph; `engines >=20` is EOL and contradicted by 15 deps requiring ≥ 22; better-sqlite3 13 requires ≥ 22 | [@monoes/monograph](#monoesmonograph-monoesmonograph-v164) |
| **9** | 113 dashboard controls throw `ReferenceError` in the shipped 2.10.30 (verified live) (quick win) | [Dashboard](#dashboard-neural-control-room-part-of-monoesmonomindcli) |
| **9** | Crash reporter is default-on (`crash-reporter.ts:109`) and files **public GitHub issues under the user's own account** containing stack traces (quick win) | [CLI Commands](#cli-commands-diagnostics-security-hooks--ops-part-of-monoesmonomindcli) |
| **9** | The README Quickstart's first command, `/mastermind:autodev` (plus `build`, `tdd`, `architect`, `approvev1`, `runorgv1`), has no file in either command tree — the whole "Autonomous Build Loop" section describes a retired command generation (quick win) | [Documentation](#documentation) |
| **9** | Every `browse` command idles ~5 s before exit (`bin/cli.js:372` `FORCE_EXIT_MS` while monobrowse's CDP/undici handles keep the loop alive): 5,133 ms via `monomind` vs 72 ms via the package's own CLI (quick win) | [@monoes/monobrowse](#monoesmonobrowse-monoesmonobrowse-v108) |
| **8** | `browse console`/`errors`/`har`/`network requests` hold per-process state and are always empty from the CLI ("No page errors" on a throwing page) — yet the shipped `agent-browser-testing` skill prescribes them after every action | [@monoes/monobrowse](#monoesmonobrowse-monoesmonobrowse-v108) |
| **8** | `monograph_suggest task=` — the tool CLAUDE.md mandates before every 3+ file task — returns "No suggestions… Run monograph_build first" on a fresh graph for any code task (quick win) | [@monoes/monograph](#monoesmonograph-monoesmonograph-v164) |
| **8** | Test lambdas drown production callers in every `context`/`impact`/`neighbors` listing (`decide`: 20 of 51 shown, all tests; `buildAsync`: 34 of 36) — an agent concludes "only tests call this" (quick win) | [@monoes/monograph](#monoesmonograph-monoesmonograph-v164) |
| **9** | Cost figures understated ~3×: Fable models have no pricing row in any of the four hand-synced tables (→ $0), Sonnet 5 priced at 4.6 rates, nothing says "unpriced" (quick win) | [CLI Commands](#cli-commands-diagnostics-security-hooks--ops-part-of-monoesmonomindcli) |
| **8** | The default hook set persists every tool input, Write body, and user prompt verbatim at 0644 (`event-logger.cjs`), the shared redaction module has zero hook callers, and init's generated `.monomind/.gitignore` leaves those stores committable (reproduced) (quick win) | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **8** | The hardened stdio MCP loop in `mcp-server.ts` is dead code: both shipping entrypoints run unhardened copies that double-wrap results, so `isError` is lost and every payload inflates 9–27% (quick win) | [MCP Tools & KG](#mcp-tools--knowledge-graph-memory-part-of-monoesmonomindcli) |
| **8** | Phantom references: 42 nonexistent MCP tools and 4 nonexistent CLI groups in shipped agents/commands (~1,000 hits), 38 of 70 routing `agentSlug`s with no file, the `guidance_*` catalog's 30 phantom tools — one reference-integrity lint | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **8** | The generated CLAUDE.md ships wrong instructions into every project × 5 platforms: `init --wizard` (nonexistent flag, silently runs a plain init), "MUST init monoswarm", `hooks session-start` (deprecated), wrong counts, inert env vars | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **8** | Generated `.mcp.json`, post-commit hook, and every doc snippet use `npx monomind@latest` — measured 70 s hang then failure with the registry unreachable | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **8** | `monomind init` re-prefixes its own output on every run (`monomind-monomind-monomind-*.md`): 600 untracked files, 399 duplicates already tracked, `.opencode/*` symlinked into `.claude/`, a manifest-only sweep | [Init System](#init-system--multi-platform-integration-part-of-monoesmonomindcli) |
| **8** | Default-exposed `config_set` MCP tool rewrites `.monomind/config.json` in a different schema and silently deletes the provider config incl. API keys (verified live) (quick win) | [MCP Tools & KG](#mcp-tools--knowledge-graph-memory-part-of-monoesmonomindcli) |
| **8** | The monoes.me OAuth access token is written as a literal `Bearer` header into the project's `.mcp.json` — the file Claude Code expects teams to commit (quick win) | [Dashboard](#dashboard-neural-control-room-part-of-monoesmonomindcli) |
| **8** | SSH federation interpolates LLM-authored message bodies into a remote shell string (`remote.ts:65-68`; `JSON.stringify` doesn't escape `$(…)`/backticks) — a prompt-injected role gets command execution on the federated host (quick win) | [Org Runtime v2](#org-runtime-v2-part-of-monoesmonomindcli) |
| **8** | Org `PolicyEngine` only applies to the Claude SDK runner; subprocess runners run their native tools unsandboxed and inherit the daemon's entire environment, including other vendors' secrets | [Org Runtime v2](#org-runtime-v2-part-of-monoesmonomindcli) |
| **8** | Unknown subcommands print help to stdout and exit 0 in 8 of 9 command groups; unknown flags are silently accepted CLI-wide (`allowUnknownFlags: true`) (quick win) | [CLI Commands](#cli-commands-diagnostics-security-hooks--ops-part-of-monoesmonomindcli) |
| **8** | `@monoes/memory` uses deferred `BEGIN` for every write, so cross-process writers fail with "database is locked" despite `busy_timeout` — 6/30 and 9/24 parallel stores failed; `.immediate()` → 0/200 (quick win) | [@monoes/memory](#monoesmemory-monoesmemory-v1017) |
| **8** | The #223/#224 "coincidental match" fix is a coverage gate that counts stopwords — natural-language questions return 0 results in keyword mode (measured) | [@monoes/memory](#monoesmemory-monoesmemory-v1017) |
| **8** | Monoswarm: the generated CLAUDE.md says "MUST init the monoswarm" but no code links monoswarm state to Claude Code's agents — enforce the claim or stop making it | [Monoswarm](#monoswarm--agenttasksession-stores-part-of-monoesmonomindcli) |
| **8** | Monograph never resolves calls through `await import()` or barrel re-exports: 0 cross-package CALLS edges, 38% caller recall | [@monoes/monograph](#monoesmonograph-monoesmonograph-v164) |

## Package Index

| Package | npm name | Version | Role |
|---|---|---|---|
| [monomind (umbrella)](#monomind-umbrella-monomind-v2110) | `monomind` | 2.11.0 | Thin shim entry point; repo tooling, build, CI, dependencies |
| [Org Runtime v2](#org-runtime-v2-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | Autonomous multi-agent "organizations" daemon; 14 runner backends |
| [MCP Tools & Knowledge Graph Memory](#mcp-tools--knowledge-graph-memory-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | MCP tool surface (25 modules, 3 stdio loops), memory knowledge graph, Second Brain docs |
| [Monoswarm & Agent/Task/Session Stores](#monoswarm--agenttasksession-stores-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | JSON-backed coordination/vote bookkeeping and agent/task/session registries |
| [Init System & Multi-Platform Integration](#init-system--multi-platform-integration-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | Project scaffolding; generated hooks, statusline, CLAUDE.md, `.gitignore`, and shipped content |
| [CLI Commands: Diagnostics, Security, Hooks & Ops](#cli-commands-diagnostics-security-hooks--ops-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | The command dispatcher, `doctor`, `security`, `tokens`, `hooks`, `memory`, `config`, gates, crash reporter |
| [Dashboard (Neural Control Room)](#dashboard-neural-control-room-part-of-monoesmonomindcli) | part of `@monoes/monomindcli` | — | `monomind ui` — 122 API routes, 12 views, auto-started on SessionStart |
| [Documentation](#documentation) | `README.md`, `doc/`, generated templates | — | The deployed site, concept docs, env/tool references, privacy wording |
| [@monoes/hooks](#monoeshooks-monoeshooks-v106) | `@monoes/hooks` | 1.0.6 | Standalone hook registry/executor + background workers |
| [@monoes/memory](#monoesmemory-monoesmemory-v1017) | `@monoes/memory` | 1.0.17 | SQLite + embeddings memory backend, HNSW ANN, chunker |
| [@monoes/monograph](#monoesmonograph-monoesmonograph-v164) | `@monoes/monograph` | 1.6.4 | Tree-sitter code knowledge graph engine |
| [@monoes/routing](#monoesrouting-monoesrouting-v104) | `@monoes/routing` | 1.0.4 | Keyword + semantic task-to-agent routing |
| [@monoes/mcp](#monoesmcp-monoesmcp-v104) | `@monoes/mcp` | 1.0.4 | Standalone MCP protocol engine (stdio/HTTP/WS) |
| [@monoes/monobrowse](#monoesmonobrowse-monoesmonobrowse-v108) | `@monoes/monobrowse` | 1.0.8 | Native CDP browser automation |
| [@monoes/monodesign](#monoesmonodesign-monoesmonodesign-v126) | `@monoes/monodesign` | 1.2.6 | Frontend design anti-pattern detection + fixes |
| [monofence-ai](#monofence-ai-monofence-ai-v102) | `monofence-ai` | 1.0.2 | AI manipulation defense (prompt-injection detection) |

---

## monomind (umbrella) (`monomind` v2.11.0)

`monomind` is the umbrella package published on npm — a pure forwarding shim over `@monoes/monomindcli` (deliberately code-free after a ~27 MB double-publish incident). The repo root also hosts the pnpm workspace, the build and release scripts, CI workflows, the dependency policy, the crash reporter's entry point, and the documentation (now its own section).

**Version note:** a concurrent session released 2.10.31 and then 2.11.0 while this milestone.md was being built — none of that release's own changes are reviewed here. Findings below that cite a specific version ("verified live" against 2.10.30, the publish-tag mismatch item) describe what was true when tested against that version; they haven't been re-verified against 2.11.0 and shouldn't be read as still-current unless re-checked.

### Achieved Milestones
- Umbrella/CLI split with a resolution shim robust across nested, hoisted, global, and pnpm-linked layouts; lockstep version + double-publish prevention (`scripts/check-publish-versions.mjs`); doc-count drift automation for two counters; git hooks auto-activation in-repo (the activated `commit-msg` hook is inert — see Future).
- **Fixed (2026-09-16):** root `pnpm build` now runs `pnpm -r run build`, building all nine workspace packages (CLI included — previously skipped entirely — plus `@monoes/monodesign` and `monofence-ai`, also previously skipped) in correct topological order, in ~1.7 s; the old orphan `dist/packages/**` tree is gone (the trailing bare `tsc` that produced it is replaced by a dedicated `typecheck` script). A new `pnpm verify` (build + typecheck + lint + every package's tests, including the three `vitest.workspace.ts` doesn't cover) is the single command to run after any change; `pnpm lint` and `pnpm run typecheck` are now also CI steps in `tests.yml`'s root job. **Corrected (2026-09-16, same day):** a review pass caught that `Typecheck` had been placed before the dependency-build step in that same commit — root typecheck scans every package under `packages/**`, not just the CLI's own dependency chain, and `@monoes/monobrowse/src/cli/platform.ts` self-imports `@monoes/monobrowse`, which only resolves once that package's own `dist/` exists; the job would have failed with "Cannot find module" on a genuinely fresh checkout. Verified by clearing every package's `dist/` and rerunning the corrected step order (`Lint` → `Build CLI's workspace dependencies` → `Build monobrowse` → `Typecheck`) end to end.
- `pnpm lint` fixed from 45 errors to 0 (`pnpm lint:fix`; pure formatting/import-order, re-verified against the full test suite with no behavior change) and is now part of `pnpm verify` and CI.
- Build and test are fast on TypeScript 7 (native): CLI package build 0.9 s, `tsc --noEmit` 0.25 s; the full test set passes on main — routing 98, hooks 64, memory 125 (+46 skipped), mcp 106, monofence 136, monobrowse 249, monodesign 1,024 (+9 skipped), root `tests/` workspace suite 5,858 (+56 skipped), full `pnpm verify` end to end ~1m45s. No failing test on main.
- Three-workflow CI suite (`tests.yml` root + 9-package Linux + 5-package Windows matrix; `publish-smoke-test.yml` with a 4-case behaviour suite; `pages.yml`), now with lint and typecheck in the root job. Still missing: coverage, link-check, snippet, license, or size gate (see Future).
- Dependency health: 0 deprecated direct deps (71 checked); `pnpm audit` 0 vulnerabilities across 542 deps; no copyleft blockers in the 293-package prod tree; all 23 CLI optional deps loaded lazily; the three ignored build scripts verified harmless.
- Outbound egress audited: code, documents, memory, and prompts stay local unless the user configures a provider or clicks "Upload org"; default-on egress is the 24 h-throttled npm update check, `doctor`'s `npm view`, and the crash reporter.
- Active release cadence; backlog 8 open / 176 closed; the 7 external reports cluster on generated hooks breaking the host session and install breakage.

### Future Milestones
- **[7/10]** (quick win) The test suite litters `~/.monomind` and `/tmp` on every run: +29 project dirs per run under `~/.monomind/projects` (`mm-intel-*`, `agent-ops-test-*`, `doctor-cmd-test-*`, `hooks-post-command-test-*`; 122 dirs / 173 MB accumulated), `~/.monomind/orgrt-operator` touched, ~150 undeleted `/tmp/{sr-test,pol,sess,mono-dag-test,…}-*` dirs; only 3 of 155 home-touching test files set `MONOMIND_HOME`. A shared vitest setup file that points `MONOMIND_HOME`/`HOME` at a per-run tmp dir and removes it; `doctor --prune-home`.
- **[7/10]** (quick win) Tarball smoke test: import every `dist/src/commands/*.js` and `mcp-tools/**/*.js` entry point, run an `initialize → tools/list` stdio handshake, run once with `--omit=optional`, use the Node `22/26` matrix, assert a size budget (cli ≤ 18 MB unpacked, monograph ≤ 30 MB). Four "uninstallable release" incidents (#130/#148, #16–#21, #17/#46/#47) got past the current suite; #36/#39 were install-time MCP stdio failures it never exercises.
- **[6/10]** (quick win) The CLI depends on npm copies of its own siblings: `@monoes/monobrowse ^1.0.6` and `@monoes/monodesign ^1.2.2` come from the registry (lock pins 1.0.6/1.2.2) while the workspace has 1.0.8/1.2.6 — verified at runtime: `monomind browse` and `monomind design` resolve `node_modules/.pnpm/@monoes+monobrowse@1.0.6` and `…monodesign@1.2.2`, so every user-facing run in this repo exercised stale published code. `workspace:^` for both.
- **[6/10]** (quick win) No `CONTRIBUTING.md`, `CODEOWNERS`, `.editorconfig`, or editor/launch config; README has no development section; root `CLAUDE.md` says `npm run build` (doesn't build the CLI) and `npm test` (watch mode); `packages/@monomind/cli/CLAUDE.md` (24 KB) has no build/test/lint commands and tells agents to run `hooks post-edit --train-neural true` and train "neural patterns" the root says don't exist; the issue template is GitHub's stock web/mobile one ("Smartphone: iPhone6"). A 60-line `CONTRIBUTING.md` (pnpm 10, Node 22/26, `pnpm install` → build → `cd packages/X && npx vitest run <file>`, `pnpm lint:fix`, the worktree rule), linked from README and both CLAUDE.md files; a CLI-shaped issue template asking for `monomind doctor` output and the platform.
- **[5/10]** (quick win) Coverage-baseline gate in `tests.yml` (lint and typecheck now run there — see Achieved; coverage is still off everywhere), plus a license-allowlist step.
- **[6/10]** The `overrides` force `onnxruntime-node`/`sharp` versions transformers never declared (3.8.1 pins ORT 1.21.0 exactly; the floor forces 1.27.0; v4 pins 1.24.3 and `sharp ^0.34` vs the `>=0.35.4` floor, blocking the upgrade) — inferred from pins, not a measured failure; verify with an embedding smoke case, then scope or drop the two floors. Express 4 and 5 are both in the prod tree (mcp and monograph on 4, the MCP SDK on 5): migrate to 5 + helmet 8.
- **[6/10]** (quick win) CLI tarball dead weight: 654 `.map` files = 3.70 MB (21%, nothing enables source-map support), `dist/tsconfig.tsbuildinfo` 439 KB, and a 2.53 MB `.claude/skills/monodesign` byte-identical to the dependency's `skill/` — ~38% of 17.55 MB removable via `files` excludes. `@monoes/monograph` is 29.6 MB unpacked, 26.1 MB of it wasm. `node_modules` is 1.9 GB; 634 MB is three packages, two optional-path only — document `--omit=optional`.
- **[6/10]** Windows CI for `cli`/`monograph`/`memory` via the win32-x64 prebuilds they already ship; run `tests/hooks` on Windows; execute every generated hook command with `{}` on stdin. (The Node-floor bump lives with the ABI item under monograph.)
- **[5/10]** (quick win) Delete or fix `.githooks/commit-msg`: its guard `[ ! -d "packages/@monomind/cli/node_modules" ]` is cwd-relative and worktree-blind (it printed "Skipping: deps not installed" on every commit made from a worktree tonight), and its only rule fires on branch `improve/auto` and rejects the `fix:` prefix the PR template requires. Either remove it or make it a conventional-commit check on every branch using `git rev-parse --show-toplevel`.
- **[5/10]** (quick win) `pnpm dev` (`tsx watch src/index.ts`) and `npx tsx src/index.ts --help` produce no output — the source entry point doesn't dispatch like `bin/cli.js`; there is no `build:watch`; running one test file from root fails (`include: tests/**`). Make the source entry dispatch when executed directly; document "run from source".
- **[5/10]** (quick win) Fold the root publish and tagging into `cli/scripts/publish.sh` (newest tag v2.10.26 vs 2.10.30 shipped); a Renovate/Dependabot group for anthropic + MCP + ai-sdk (`@anthropic-ai/claude-agent-sdk` is 47 patches behind).
- **[4/10]** Clone weight: `.git` is 191 MB, dominated by a once-committed Rust `target/` (6 × 10–15 MB blobs, now ignored but forever in clones), 26 MB tracked wasm, 12 MB PNGs. Not worth a history rewrite; move wasm grammars to a release asset/`postinstall` download, shrink PNGs. Dead config (`allowedVersions` for `marked`/`vitest`/`@vitest/coverage-v8`; `@types/express 5` on express 4; a second 28 MB `tree-sitter-cli`); `pnpm test:all` omits two packages; test stdout pollutes the dot reporter; `tests.yml:13` says "~1,200 CLI tests" (3,061).
- **[3/10]** (quick win) Dead-pin audit for the 28 overrides; root `npm pack --json` broken by `prepare` printing to stdout; monobrowse publishes `src/__tests__`.

---

## Org Runtime v2 (part of `@monoes/monomindcli`)

Org Runtime v2 is a daemon-coordinated system for running autonomous multi-agent "organizations" — role-based teams of live, provider-backed AI sessions (a boss plus specialist roles) that communicate over async mailboxes, persist resumable state via checkpoints, and can be driven by interchangeable LLM backends, with fault tolerance, cross-org/cross-process messaging, and a full audit trail (`bus.jsonl`). Calibration note: most items here affect only org-runtime users on non-Claude runners — a small share of the base — and are scored accordingly.

### Achieved Milestones
- `OrgDaemon` lifecycle (~1076 lines): start/stop/deliver/resume, lazy role spawning, atomic `spawning` guard; `respawnRole` and `Mailbox` push/shift verified race-free.
- 14 runtime ids over 14 files (Claude SDK + 13 subprocess/SDK runners). Per the round-2 matrix: `maxTurns` honored by claude and vercel only; provider-session resume for 8 runners, not for 6; usage/cost real for 6 of 14.
- "Fence Protocol" (`tool-fence.ts`) for the 12 runners without native tool-calling; per-role provider/runtime resolution; mixed-runtime orgs.
- Checkpoint/resume: captured on stop/crash only; atomic `runtime.json` writes; v1→v2 migration and validation tested. Survives resume: unconsumed mailbox queues, usage incl. retired usage, generations, role overrides, the task DAG, gates and questions. Doesn't: the in-flight message, worktrees, pending approvals, conversation state on 6 runners.
- Per-role `PolicyEngine` enforced through the Claude SDK `canUseTool` hook only. Fault tolerance: bounded boss restarts, circuit breaker, idle watchdog, stale-PID detection, serve lock, orphan SDK reaping. Cross-org/cross-process messaging with sender-identity verification on `xdeliver` (`cross-org.ts:598-608`); bus `audit` events redact and truncate tool inputs; SSH `BatchMode=yes` fails closed on unknown hosts.
- Tests: every runner has a dedicated (fake-child/fixture) test file; 60+ orgrt test files; no fake runner exists.

### Future Milestones
- **[8/10]** (quick win) SSH federation interpolates agent-authored message bodies into a *remote* shell string. `remote.ts:65-68` builds `` `cd ${JSON.stringify(host.cwd)} && npx monomind org inbox ${orgName} --json ${JSON.stringify(payload)}` `` as one argv to local `ssh` (the local shell was fixed in SEC-4), but sshd hands it to the remote login shell, which expands `$(…)`, backticks, and `$VAR` inside double quotes — `JSON.stringify` escapes none of them (verified). `body` is a role's LLM output (`cross-org.ts:268-278`), so a prompt-injected role gets command execution on the federated host as the SSH user; `orgName` is unquoted; `host.cwd` same class. Pass the payload on stdin (`execFileSync(..., { input })` + `org inbox --json -`), single-quote-escape `cwd`, validate `orgName` with the broker's `SAFE_NAME`; contract test with a `$(…)` body against an `ssh` shim. (Supersedes "`deliverRemote()` has no tests".)
- **[8/10]** Enforce role policy on subprocess runners and stop leaking the daemon's environment: `PolicyEngine.decide()` runs only through the SDK `canUseTool` hook; Codex launches with sandbox mode `danger-full-access` (`codex-runner.ts:392`), Antigravity with its permission-bypass flag, Qwen `--yolo`; `resolveProviderEnv` starts from a full copy of `process.env` (`provider.ts:14-17`) and every runner spreads `{...process.env, ...args.env}` again — a Codex role runs with `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GITHUB_TOKEN`, `AWS_*` and an unsandboxed shell. Map policy → native sandbox flags where supported, OS sandbox otherwise; child env from an allowlist; `org validate` warns and `org run` requires `--accept-unsandboxed`; enforcement matrix; scrub-on-spawn test per runner.
- **[7/10]** Checkpoint fidelity — "resumable" doesn't hold. (a) `finishStop` force-removes shared and per-role worktrees (`daemon.ts:2294-2318`) and resume recreates them from HEAD — every stop→resume in worktree modes destroys uncommitted role work. (b) `Mailbox.serialize()` copies `queue` only (`mailbox.ts:202-209`) and capture happens before the abort — the in-flight message is dropped. (c) Pending approvals: the daemon's Map starts empty on resume (`org approve` 404s); `clearApprovalsForFreshStart` has zero callers; the dashboard's approve route (`routes-org.mjs:1770-1791`) edits `approvals.json` without contacting the daemon, which rewrites the file from memory and never reads it back; and the dashboard's live "answer question" forward (`routes-org.mjs:1695-1700`) sends no credential against the daemon's operator-only route (`server.ts:20-25,180-188`), so it always 401s. Keep worktrees on stop, record `worktreePath` and re-attach; serialize `inFlight`; the daemon owns approvals and the dashboard calls its routes with the operator credential; tests for each. Note the operator boundary is user-level: `~/.monomind/orgrt-operator/<org>.json` is 0600 but readable by any Bash-capable role running as the same user.
- **[6/10]** (quick win) `org serve` shuts down as a crash: its signal handler calls `persistCrashStateAll()` then `process.exit` (`org.ts:1313-1325`) while `org run`'s resolves into `daemon.stopAll()`. `await daemon.stopAll({ drainMs })` with a hard-exit fallback.
- **[6/10]** (quick win) Vercel roles fragment the org bus per token (`vercel-runner.ts:137-143` yields per `text-delta` with no `includePartialMessages` gate; `session.ts` treats each as a turn) — and every bus event is then forwarded one at a time: `forwarder.ts:130-160` does a `readFileSync(dashboard-token)` plus one serialized HTTP POST with a 3 s timeout per event, and the dashboard side appends per event then rewrites the whole `run-events.db` via `_runDb.export()` on a 1 s debounce (`server.mjs:440-446`). The bus itself is cheap (append 24 µs/event, no fsync, `bus.ts:56-62`; a synthetic 52 MB / 150k-event bus reads and parses in 125 ms; `runtime.json` is written only at start/stop/crash/heartbeat), so per-token fragmentation is expensive only because of the forwarder. Accumulate per step; batch the forwarder (flush every 250 ms or 50 events) and cache the credential; make the run-db persist incremental or interval-bounded; contract test.
- **[6/10]** Abort and timeout kill only the direct child (verified with a fake `codex`: the runner stayed blocked until a grandchild released stdout, and the failure read as a crash). Spawn detached, kill the process group, destroy stdout on SIGKILL, tag `err.aborted`.
- **[6/10]** Per-cwd session stores bleed across roles (pi/pi-rpc `--session-dir` and crush `--continue` keyed by cwd) — key by `<orgDir>/runners/<roleId>/` as vercel does. Six runners restart cold on resume and nothing says so — inject a bounded prior-transcript block; emit a `resume-context-lost` audit.
- **[6/10]** Liveness and runner-contract harness (a fake runner asserting the three invariants; a parameterized per-runner contract test) — the ~45-issue liveness cluster keeps re-breaking because none of it is testable today. Nine hand-copied `streamTurn`s already disagree on timeouts and exit semantics (only hermes treats `timedOut` as failure; Vercel has no timeout; `maxTurns` ignored by 12); unparsable stdout lines are dropped uncounted and empty exit-0 turns pass as success. Extract `SubprocessTurn` + `runFenceLoop`; one timeout matrix.
- **[6/10]** (quick win) TTL and validation failures are opaque hard refusals with no checkpoint age in `org status`; `run_config.prechecks` only run for `org serve`; `org validate` checks schema only (call `scanInstalled()`; publish a per-runner verification ledger). Periodic checkpoints and bus rotation after the fidelity item (`OrgBus.readHistory()` also drops a torn last line silently).
- **[5/10]** (quick win) Six runners pass the whole prompt as one argv element (verified `E2BIG` thrown synchronously at 129 KiB, past the error handler; fed-back tool results uncapped). Copilot tool rounds are contextless. Resume against an edited config silently drops roles (a renamed boss → no boss). Vercel/Hermes cost token-only, copilot/crush zero. Document the seven undocumented runners as the matrix; fix `org-runtime.md` §4.5. Human-decision inputs unbounded (no cap on `answer`, no `questionId` format check, no rate limit on either server).
- **[4/10]** (quick win) `classifyStderr` marks turns FATAL on any `401`/`403` substring (shared by 8 runners); `checkpoint-recoverable-close.test.ts` tests `mergeCheckpoint`, which production never calls; `org serve`'s mutual exclusion has a pre-heartbeat startup window (`org.ts:1214` vs `:1335`); Gemini CLI subscription runner and the deprecated `provider.kind: 'gemini'` fall-through.

---

## MCP Tools & Knowledge Graph Memory (part of `@monoes/monomindcli`)

The MCP surface `@monomind/cli` exposes to Claude Code and other clients (`mcp-tools/**`, 25 modules; three stdio request loops), plus the two knowledge stores it fronts: the memory knowledge graph (`memory-kg.ts`) and the Second Brain document index (`document-pipeline.ts`), fused by `query-router.ts` behind `knowledge_search` and `doc search`.

### Achieved Milestones
- 25 modules; 252 tools defined (module and tool counts predate the graphify_* removal below — see the header caveat); `tools/list` advertises 67 by default (30,190 bytes ≈ 7.5k tokens per session), 219 under `MONOMIND_MCP_FULL=1`. Measured latency: `initialize` 20–23 ms; `monograph_query` 6.9 ms, `knowledge_search` 4.7 ms, `memory_kg_search` 0.9 ms medians; no hangs or crashes on 12 malformed calls. (Measured against the 2026-09-09 dist — see the header caveat.)
- **Fixed (2026-09-16):** the 14 deprecated `graphify_*` MCP tool shims (`mcp-tools/graphify-tools.ts` and its test) are deleted, not just documented as slated for removal — `mcp-tools/index.ts` and `mcp-client.ts`'s category-loader map no longer reference them. Per the user's explicit call: no project's existing `.monomind/monograph.db` needs to survive this, since `init --force` already rebuilds it unconditionally (see Init System).
- Monograph reads during a build are sound: 214 `monograph_query` calls across a forced rebuild, 0 errors, p95 5 ms.
- Module verdicts: **real** — `system`, `platforms`, `terminal` (the best-tested module), `browser` (thin wrapper over `@monoes/monobrowse`), `performance`, `analyze`, `transfer`, `claims`; **partial** — `config`, `guidance`, `github`, `embeddings`, `autopilot`. Of 67 core descriptions, 29 say what and when, 35 are stubs, 3 are wrong.
- Second Brain pipeline: content-hash idempotent ingest verified (single-writer); heading/fence-aware chunking on the cut side; multi-surface router with RRF; measured keyword-mode quality 5–6 of 10 at rank 1. Memory KG ingest/rollback/integrity verified on a 10-triple round trip; org-scoped via `KgScope`.
- An eval harness exists (`doc eval`) but has never produced a number in this repo state. Input validation through `input-guards.ts` on `knowledge_*`, `browser_*`, `config_import` — but rejections are currently reported as success (next item).

### Future Milestones
- **[8/10]** (quick win) The hardened stdio loop is dead code and two unhardened copies ship. `src/mcp-server.ts:598-679` (name validation, `-32602` checks, prototype-pollution sanitizer, `isMcpResult` passthrough) is never on the stdio path: `mcp start` — the `.mcp.json` command — runs the loop in `packages/@monomind/cli/bin/cli.js:187-204`, and the `monomind-mcp` bin runs `bin/mcp-server.js:148-177`; both `JSON.stringify(result, null, 2)` unconditionally. Verified on both: `tools/call` with `name: 42` → "name.indexOf is not a function"; every MCP-shaped handler result comes back as `content[0].text = "{\n \"content\": [...], \"isError\": true}"` — **`isError` is lost, so a `knowledge_ingest` path-traversal rejection reads as success**, and outputs inflate 9–27% per call. Export `handleMCPMessage`, have both bins delegate, delete the copies, drive the test harness over `bin/cli.js mcp start` with one snapshot per response shape, one `getPackageVersion` for the three `3.0.0` literals.
- **[8/10]** (quick win) `config_set` destroys the CLI provider config: `config-tools.ts:53-94` reads `.monomind/config.json` as `{values, scopes}` and `saveConfigStore` rewrites the file with only that shape, while `config-file-manager.ts:11,352` stores `providers[].apiKey` in the same file. Verified live. Facade over `configManager`; test that providers survive a `config_set`. Follow-up: keys in `~/.monomind/credentials.json` or the OS keychain with a `keyRef` in project config.
- **[7/10]** 40% of the per-session roster is admin bookkeeping nobody consumes: `agent_*`/`task_*`/`session_*` (20 tools ≈ 1,660 tokens), `config`, `system`, `guidance` ≈ 3,000 of the 7.5k tokens every session pays; 68% of the bytes are per-property schema descriptions. Discovery-only for the bookkeeping tools, `mcp.roster: minimal|core|full`, cap property descriptions; hide the overlapping `memory_session-*` and `memory_kg_search` (already fused by `knowledge_search`); target ≤ 4k tokens.
- **[7/10]** (quick win) Silent keyword-only ingest is permanent: with no embedding model cached (every fresh install until the first ~90 MB download), `doc ingest` stores un-embedded chunks silently and content-hash dedup never re-embeds them; `doc search` help still says "Semantic search". Print `method: keyword`, `doc reindex --missing-embeddings`, `doctor` reports model presence; cache the model under `~/.monomind/models` with `HF_HUB_OFFLINE` support (prerequisite).
- **[6/10]** (quick win) Validation errors are `-32603` "Internal" and type errors leak (`{query: 42}` → a raw TypeError; a 1 MB query accepted; over-length says "query is required"; unknown keys ignored). Reject mismatches, `maxLength` on every string prop, `{isError: true}` content so the model can self-correct.
- **[6/10]** (quick win) `mcp toggle --disable` hides nothing (`listMCPTools` returns `enabled: false` but the loops map only name/description/schema; `callMCPTool` rejects it); `listChanged: true` claimed, never emitted; `capabilities.json` flags never consulted.
- **[6/10]** Router's confident single-surface route hides documents (`query-router.ts:189-218`); excerpts show the situating blurb, not the match (`document-pipeline.ts:867-874`); the top score always displays 1.050.
- **[6/10]** (quick win) `system_health` can never report healthy (`system-tools.ts:423-435`). `terminal_execute` opt-in in a project-writable file with `execSync(command)` behind a denylist.
- **[5/10]** (quick win) `doc eval` is unrunnable offline and measures a non-production model; `github_repo_analyze`/`github_metrics` return fabricated zeros (pipes passed as literal argv); 35 of 67 core descriptions are stubs and 3 are wrong (`hooks_pre-task` "ADR-026 routing" never shipped, `hooks_route` "native HNSW", `agent_terminate`/`agent_health` status-field edits) — a description lint. KG search ignores relations and isolated nodes, display names last-writer-wins, no diacritic folding, `kgStats` double-counts. `autopilot_*` promises a Stop hook nothing installs. `embeddings_search` mislabels its index. Cold-start warm-up. Opt-in entity extraction on ingest (after the KG search gaps).
- **[4/10]** `claims_*` (no consumer, cwd-relative path); `browser_*` can't launch Chrome although monobrowse can (unmeasured); Memory Palace duplicates the KG; `store: 'global'` silently drops surfaces; remote MCP `corsOrigins: ['*']`; read-only tools create `.monomind/{github,performance,system}/`; the project store still lives in a `lancedb/` directory.

---

## Monoswarm & Agent/Task/Session Stores (part of `@monoes/monomindcli`)

The "monoswarm" coordination feature — 13 `monoswarm_*` MCP tools, the `agent_*`/`task_*`/`session_*` tools, and the matching CLI groups. Every tool here is JSON bookkeeping: `agent_spawn` starts no process, votes are tallied in-process, and no code links monoswarm state to Claude Code's Agent/Task tool. The only runtime consumer is the statusline's swarm dot — and the 20 `agent_*`/`task_*`/`session_*` tools cost ≈ 1,660 tokens in every session's `tools/list`.

### Achieved Milestones
- 13 `monoswarm_*` tools over one `<dataRoot>/monoswarm/state.json` (`.git/monomind` in a git repo); honest "starts no process" descriptions; real vote math with deadlock detection, flip invalidation, duplicate check, divergence gate; HMAC-signed audit trail. `agent_spawn`/`task_*`/`session_*` persist JSON stores with corrupt-store refusal. `.monomind/registry.json` (138 entries, schema-complete, content-empty — see Future). 59 tests across 7 files.

### Future Milestones
- **[8/10]** Decide what monoswarm is — enforce the claim or stop making it. `claudemd-generator.ts:151-157` emits "MUST initialize the monoswarm for complex tasks…" into every user's CLAUDE.md; reality: init writes 507 bytes only the statusline reads, Task-tool agents never appear in `agents/store.json`, roster agents never run, and the shipped `monoswarm-*` agent bodies still call claude-flow-era tools. Either honest-down (drop the MUST, move the 20 bookkeeping tools out of the core roster) or build the link via the `SubagentStart`/`SubagentStop` hooks so the electorate = live Task agents.
- **[7/10]** (quick win) `session restore --latest` returns garbage (hook-session archives share `<dataRoot>/sessions/` with `session-tools.ts`; with no real saves it sorts by an undefined `savedAt` and returns `restored: true` for an empty file) — the command the generated `cli/CLAUDE.md` tells every session to run.
- **[5/10]** (quick win) Three CLI → tool wiring bugs (`--strategy` dropped; `start` re-inits with a hardcoded topology; `status` counts a directory nothing writes); the registry has `capabilities: []` on 138/138 entries and 49 descriptions shared by two slugs; consensus code untested and split across two data roots; docs describe a different product (path, strategies, `queen`, `/mastermind:swarm`); `agent_spawn` advertises routing it cannot do.

---

## Init System & Multi-Platform Integration (part of `@monoes/monomindcli`)

The init system (`packages/@monomind/cli/src/init/`) scaffolds a project for Claude Code and other agentic coding platforms and generates the hook scripts, the statusline, the CLAUDE.md, the `.monomind/.gitignore`, and the agent/skill/command content that then run inside the host session. This section owns hook latency and safety, whether hook output reaches the model, what the hooks persist, statusline truthfulness, and the correctness of everything it generates.

### Achieved Milestones
- Five fully-wired platform targets (four more registry ids install nothing silently); isolated per-platform directories; security-gate parity per platform (with the fallback-template caveat below); three presets + wizard; `init upgrade` with a manifest; forced Monograph rebuild on `init`/`--force`/`--wizard`.
- Measured fresh install: 2.6 s, 1,845 files / 19.3 MB; generated CLAUDE.md ~2.4k tokens per prompt; 0 of 712 shipped content files fail to parse. Root and packaged helpers are byte-identical (the *generated* fallbacks are not).
- Generated hook scripts are robust to bad input (every helper exits 0 on malformed stdin); measured latency 25–128 ms per handler, 75–80 ms for the statusline. Statusline fields verified accurate: version, git status, graph counts and freshness, doc/chunk counts, dashboard status. Verified inaccurate: cost, graph-assist share, model label.
- SessionStart chain measured hook by hook (5 runs each, realistic stdin, shipped `MONOMIND_HOOK_QUIET=1`; `node -e 0` baseline 15 ms): `mastermind-activate` 17 ms, `monolean-activate` 17, `session-restore` **523** (max 549, 15 s budget), `monograph-freshen` 80, `control-start` 31, `event-logger` 24 — ≈ 690 ms serialized, ≈ 530 ms if the host runs same-event hooks in parallel. The first session after 6 h adds the staleness-gated workers inline (map 286 + audit 164 + consolidate 78) → ≈ 1.05 s, consistent with the earlier 2.8 s max on a cold page cache. Nothing in the chain is broken; what it spends the time on is the next item.
- **Fixed (2026-09-16):** `capture-handler.cjs`'s `readStdin()` timer (both the root and packaged copies) now `.unref()`s, so the process no longer lives 3 s past `resolve()`. Regression-tested (`tests/hooks/capture-handler.test.mjs`): the whole file's 12 tests, including 3 subprocess start/stop round-trips that previously needed a 30 s vitest timeout override, now run in 440 ms total. The per-tool-call `Bash|Write|Edit|MultiEdit` 2 s/3 s-budget entries in this repo's own and the packaged `settings.json` are a separate, still-open issue (see Future).
- **Fixed (2026-09-16):** `monograph-freshen.cjs`'s (formerly `graphify-freshen.cjs`) `resolveMonographEntry()` (all three copies — root `.claude`, `.gemini`, packaged) no longer runs `execSync('npm root -g')` eagerly; it's now tried only after the four fast path-existence candidates and the pnpm-store candidate all miss. Regression-tested: a fake `npm` shim on `PATH` proves the shim is never invoked when an earlier candidate resolves (`tests/hooks/monograph-freshen.test.mjs`).
- **Fixed (2026-09-16):** Dropped "graphify" — a distinct external project's name — from every place this product used it as if it were its own branding: the SessionStart hook `graphify-freshen.cjs` → `monograph-freshen.cjs` (root `.claude`, `.gemini`, and packaged copies, plus every generator/wiring reference: `settings-generator.ts`, `helpers-generator.ts`, `executor.ts`, `doctor-project-checks.ts`, `session-restore-handler.cjs`, `utils/monograph.cjs`, `.claude/settings.json`); `InitComponents.graphify`/`MCPConfig.graphify` → `.monograph` (`types.ts` and every call site: `init.ts`, `init-wizard.ts`, `init-subcommands.ts`, `executor.ts`, `settings-generator.ts`); `graphifySection()` → `monographSection()` (`claudemd-generator.ts`); `getGraphifyStats()` → `getMonographStats()` (`statusline-generator.ts` and the three shipped `statusline.cjs` copies); a stray `// Graphify knowledge graph suggestion…` comment (`helpers-generator.ts`) and `doc/concepts/statusline.md`. Deliberately left alone: the historical `/graphify` phantom-slash-command incident comments in `router.cjs`/`build-skill-registry.cjs`, the `graphify-out` legacy on-disk directory name in `routes-monograph.mjs` (reads old user data that may genuinely exist under that name), and the "Mirrors graphify's `_normalize_id`/`transcribe.py`/…" attribution comments in `@monomind/monograph/src/{graph/normalize-id.ts,ingest/query-memory.ts,ingest/transcribe.ts,pipeline/phases/communities.ts}`, which credit a real, separate open-source project this code ported algorithms from — not this product's own former name. Verified: fresh `init` now wires `.claude/settings.json`'s SessionStart hook to `monograph-freshen.cjs` and the generated statusline runs cleanly; full `pnpm verify` green.
- **Fixed (2026-09-16):** a project initialized before the rename above now migrates cleanly instead of accumulating dead weight or a broken hook. `doctor` gained a check (`checkHelpersFresh`, via a new `OBSOLETE_HELPER_NAMES` list) that detects a pre-rename helper still on disk and points at `monomind init --force`; that `--force` run now (a) deletes the obsolete file from `.claude/helpers/` and `.gemini/helpers/` (`writeHelpers`) and (b) strips its command from `settings.json` before the existing hook-preserving merge runs (`stripObsoleteHookCommands`, `writeSettings`) — without (b), the merge's own by-design "unknown hooks are preserved, never dropped" behavior (see `mergeHooksPreservingUnknown`'s docstring) would have kept the old command as a *duplicate* SessionStart entry pointing at the now-deleted file, on every project that had ever run `init --force` before this fix. Verified end to end: a simulated pre-rename project (old hook file + old settings.json command + a hand-added custom hook) goes from `doctor` warning to a clean pass after one `init --force`, with the custom hook still intact. Regression-tested (`write-helpers-force-removes-obsolete.test.ts`, `write-settings-force-drops-obsolete-hooks.test.ts`, a new `doctor-project-checks.test.ts` case). At the user's explicit direction, no attempt was made to preserve or migrate an existing `.monomind/monograph.db` — `init --force` already rebuilds it unconditionally.

### Future Milestones
- **[9/10]** Almost no hook enrichment reaches the model. (a) `settings-generator.ts:59` ships `MONOMIND_HOOK_QUIET=1`; `route-handler.cjs:27-30` routes all 34 advisories through `advisoryLog`, which returns early — per-prompt route stdout is **0 bytes** in every init'd project (1,605 B unquiet); `MONOMIND_HOOK_VERBOSE`, which the generator's comment says opts back in, is read by nothing. (b) `[MONOGRAPH_HINT]`/`[AFFECTED_TESTS]`/`[SECURITY_EDIT]`/`[COMPACT_GRAPH]` go to plain stdout on PreToolUse/PostToolUse/PreCompact, which Claude Code feeds to the model only for UserPromptSubmit/SessionStart; `hookSpecificOutput.additionalContext` appears nowhere. (c) The statusline's `📊75%·25%grep 💰$2.15` is built on those unseen hints: 397 of 406 "wins" are `graph_assist_search` hook assists, 8 are real `monograph_call`s, the dollars are a fixed 1,700 tokens × $/Mtok per event (`utils/monograph.cjs:225-311`). `[BUDGET_BREACHED]` advice goes to the same muted channel. Emit `additionalContext` for tool hooks, invert the QUIET default, count only real calls in the statusline, test that each tag lands where the model reads.
- **[8/10]** (quick win) The default hook set persists everything verbatim, and init leaves it committable. Reproduced in a scratch project: a Bash command carrying a fake Stripe live key and a GitHub token, a `.env` Write body, and a prompt containing a password phrase all landed in `.git/monomind/events/*.jsonl` at 0644 (`event-logger.cjs:110-120,150,164` stores full `tool_input`, `tool_result`, prompt text, and the whole `raw` payload < 4 KiB; fallback location `.monomind/events/` when there is no `.git` at the cwd). `redaction.ts` (12 secret patterns) is called by the crash reporter and policy code but by **no hook script**. The same holds for `capture/spool`, `episodic/episodes.jsonl`, `orgs/*/runs/*/bus.jsonl` (`chat`/`xorg` full text), `runtime.json` (500 raw scrollback lines per role), `questions.json`, and `<project>/.monomind/run-events.db` + `data/mastermind-events.jsonl` (forwarded prompts; `MONOMIND_HOME` resolves to the *project* dir in `server.mjs:575-583`). And init's generated `.monomind/.gitignore` (`write-runtime-config.ts:69-83`) ignores only `sessions/ security/ *.tmp *.log daemon.pid *.key *.token *.secret .env` — `git check-ignore` in a fresh repo shows `dashboard-token` (`*.token` doesn't match it), `control.json`, `events/`, `capture/`, `episodic/`, `routing-feedback.jsonl`, `metrics/`, `bus.jsonl`, `runtime.json`, `questions.json`, `monoes-connection.json`, `last-route.json` all committable (`git add -A --dry-run .monomind` → 14 files); the repo's own root `.gitignore:59-74` ignores exactly these — the generator drifted. Port the secret patterns into `helpers/utils/redact.cjs` and apply before every append; drop the `raw` field; for Write/Edit store `file_path` + a content hash, never `content`; 0600 files; generate the repo's own ignore list (plus `*.db*`, `dashboard-token*`, `control.json`, `monoes-connection.json`); a `doctor` gitignore-coverage assertion per known store; `MONOMIND_EVENT_LOG=off|meta|full`.
- **[8/10]** Phantom references — one reference-integrity lint. Of 110 distinct `mcp__monomind__*` names referenced in `.claude/{agents,skills,commands}`, 42 (~440 hits: `swarm_init` 103, `memory_usage` 100, `coordination_orchestrate` 37, `task_orchestrate` 28, `workflow_*` 31, `neural_*` 11) do not exist, plus `monomind github/swarm/hook/workflow/pair/hive-mind` (~600 hits) — packaged, not just root; 38 of the 70 `agentSlug`s in `@monoes/routing` (the entire game-dev/product/design/marketing domains, `security-architect` — also in root CLAUDE.md — `tdd-london-swarm`) have no agent file and `route-layer.ts` never validates; `guidance_capabilities`/`guidance_recommend` (core, default-exposed) hardcode a catalog with 30 phantom tools, 12 phantom skills, 4 phantom agents, and "17 hooks + 12 workers" (28/9). A `scripts/` lint that diffs every tool name, CLI command, agent slug, and skill referenced in `.claude/**`, routing, guidance, and the CLAUDE.md generator against the registry + `cli --help` + the packaged agent set; search-replace the renames; delete or rewrite the 17 hive-mind/queen files; derive the guidance catalog from the live registry; fall back to `general-purpose` for unknown slugs.
- **[8/10]** The generated CLAUDE.md ships wrong instructions into every project × 5 platforms (verified in a scratch project's output): `npx monomind@latest init --wizard` (the flag doesn't exist — `init wizard` is a subcommand — and unknown flags are ignored CLI-wide, so it silently runs a plain init), `hooks session-start` (deprecated), "29 hook subcommands + 8 workers _(unavailable in this install)_" (28/9, available), "66+ tools" (67), "MUST init monoswarm", an env block advertising `MONOMIND_MEMORY_BACKEND`/`MONOMIND_MEMORY_PATH` (read by nothing), "Neural: Disabled"; the settings/MCP generators emit `MONOMIND_MODE/V1_ENABLED/HOOKS_ENABLED/TOPOLOGY/MAX_AGENTS`, which nothing reads. Derive the CLI section from the live command registry and the doc-count script; delete the inert env block; drop the MUST.
- **[6/10]** (quick win) The per-tool-call `Bash|Write|Edit|MultiEdit` pretool/posttool entries (2 s/3 s budgets — measured 3,021 ms on a kill) exist in this repo's own `.claude/settings.json` and the packaged `packages/@monomind/cli/.claude/settings.json:93,145`, not in generator output — a narrower remnant of the capture-handler timing item now that the `.unref()` half is fixed (see Achieved). Investigate whether these budgets are still needed now that the handler exits promptly, and drop or shorten them if not.
- **[7/10]** (quick win) `session-restore` spends 80 % of its 523 ms on three avoidable pieces — a ≈ 0.4–0.9 s saving on every session start. CPU profile (`--cpu-prof`, 551 ms, self time by file): **`token-tracker.cjs` 230 ms (42 %)** — `quickSummary()` (`:631-640`) is deliberately cache-first with a background refresh (the #42 fix), but `session-restore-handler.cjs:574` calls `quickSummaryData()` = `_computeQuickTotals()` **synchronously** four lines later to write `metrics/token-summary.json`, re-walking every transcript under `~/.claude/projects` (65 MB here; the #42 comment cites 10 s at 1 GB) for a number the model never sees; **`utils/monograph.cjs` 149 ms (27 %)** — `:635-690` opens the 191 MB graph DB, runs `COUNT(*)` on nodes and edges plus the degree query and two `git` spawns, then prints only when `MONOMIND_HOOK_QUIET !== '1'`, which the shipped settings never allow; **inline workers 65–590 ms** — `session-restore-handler.cjs:310-339` awaits ddd (every session, 65 ms) and, when stale, map/audit/consolidate serially (each capped at 1.5 s) although a detached-spawn pattern already exists four blocks up (`:196-208`). Have the detached `refresh-summary` child write `token-summary.json` and let the hook only read the cache; return early from the god-nodes query under QUIET and read counts from `index_meta` written at build; spawn all four workers detached and never await. (`monograph-freshen.cjs`'s eager `npm root -g` — fixed, see Achieved.)
- **[8/10]** `npx monomind@latest` makes the MCP server and git hooks unusable offline (generated `.mcp.json`, post-commit hook `helpers-generator.ts:56`, `control-start.cjs:93`, every doc snippet; measured 70 s hang then failure with the registry unreachable despite a warm cache). Pinned version or local path in `.mcp.json`, `--prefer-offline`, a test that generated files contain no `@latest`.
- **[8/10]** `monomind init` re-prefixes its own output on every run. On disk in this checkout: `.kimi-code/plugin/commands` has 115 bare, 115 `monomind-monomind-`, 119 `monomind-monomind-monomind-`, and 1 quadruple-prefixed file; `.claude/commands` 120 double-prefixed; `.kimi-code/skills` the same shape — 600 untracked files from one `init` run inside the repo (mtime 2026-09-15 15:19), and **399 duplicates already tracked in git** (two prior cleanup commits, `eea9c61c0`, `99b475d5c`). Three legs: the generator prefixes already-prefixed basenames; `.opencode/{agent,command,skills}` are committed symlinks into `.claude/` (`write-opencode.ts` wrote straight into `.claude/commands/`); the stale-sweep is manifest-only; and `copy-assets.ts:194-207` copies category dirs without the `isLikelyUserFile` filter, so 12–13 `README.md` files are registered as commands. Skip already-prefixed basenames, reconcile against a manifest of generated files, make init refuse or no-op when cwd is the monomind repo, replace the symlinks with real dirs, delete the tracked duplicates, filter READMEs, test "init twice → identical tree" and "no `.claude/**` path is a symlink target".
- **[7/10]** Generated helpers drift from the shipped ones: the fallback `hook-handler.cjs` template's `pre-write` handler is a stub comment (`helpers-generator.ts:574`) with a non-atomic graph-gate write; `generateStatuslineScript()` — what `init upgrade` writes — differs from the shipped `statusline.cjs` by 679 lines and serves from a global `~/.monomind/statusline-cache.json` keyed only on mode (reproduced cross-project poisoning); `hooks statusline` is a third implementation; the statusline ignores Claude Code's stdin payload (`model.display_name`, `cost.total_cost_usd`, `context_window` — the only accurate per-session numbers; model labels re-derived wrong from JSONL); `statusline-generator.ts` (1,490 lines) has no tests. One implementation, cache keyed by cwd, read stdin with a 50 ms cap, delete the fallback generators or test their output against the shipped handler.
- **[7/10]** Generated hooks must never be able to break the host session (#42, #58, #236, #129 — every external report): a publish-smoke case executing every generated hook under `sh -c` with a scrubbed PATH and a 5 s budget; fail-open wrappers for enrichment; `doctor` "hooks executable from a bare shell". Also, inferred not reproduced: `init --force` overwrites live `.claude/helpers/*.cjs` with `copyFileSync` (`write-claude.ts:291,369`, `copy-assets.ts:206`) while a hook may be mid-`require()`; `upgrade.ts:229-242` already does tmp+rename — use `atomicWriteFile` everywhere.
- **[7/10]** (quick win) Every agent ships twice with two names (88 identical root pairs; 55 packaged Title-Case names with spaces so `subagent_type`, filename, and registry slug disagree) — one file per agent, kebab `name:` = filename, ~4k tokens/prompt saved.
- **[7/10]** Graph gate blocks non-code searches (`ps aux | grep`, `ls /proc/…`, `Glob **/*.md`, `git grep`, docs greps — reproduced; one wasted turn per teammate session) — and the graph-usefulness evaluation says the block is net negative outside one case: across 8 realistic tasks the graph beat the grep the agent wanted to run in 2 (`importedBy` of a file; callers of a unique name), tied in 1 (exact-name `config_set`, bm25 mode), lost in 5 (intent queries, test-heavy symbols, every `suggest task=`), and in 2 of the 5 returned a *confident wrong answer* a grep would have prevented (see monograph). Block only when the grep pattern is a bare identifier (`^[A-Za-z_$][\w$]*$`, no `-i`/`-E`); hint without blocking otherwise; exempt Glob, non-first pipeline segments, non-source paths; log win/loss per session so the statusline's graph number is real; downgrade to `additionalContext` once enrichment works.
- **[6/10]** (quick win) 49 packaged `mastermind/*.md` commands have no frontmatter and 48 more no `description` (bare names in the model's listing); cap skill descriptions; collapse the four 93%-identical business templates. `doctor`/`init` tell every user to run `platforms doctor`, which prints nothing (a rendering bug; the MCP tool returns real evidence); `docs/platforms/compatibility.md` calls installed capabilities "experimental". `init --platform trae|antigravity|hermes|zed` writes zero artifacts yet exits 0. Default `init` writes 252 files across four other platforms into a Claude-Code-only repo and the README never mentions `--target claude`.
- **[6/10]** Upgrade-safety invariant (five data-preservation regressions since `f8e2291eb`; `init-manifest.json` doesn't gate deletion): a property test, manifest as deletion authority, publish-suite case. Ship the secret-scanning pre-commit hook to user projects.
- **[5/10]** Trim the footprint (87 skills × 3 copies; 40 backup files per re-run; `mastermind/master.md` 66 KB × 3); statusline staleness (`token-summary.json` only at SessionStart; UTC day); `learning-service.mjs` dead; hook fan-out (5 processes per Bash call); PreCompact entries with no `timeout`; the dashboard's token fan-out into other repos' `.monomind/` (`server.mjs:1054-1073`, never cleared on stop).
- **[4/10]** (quick win) `--platform` as the single selector + a wizard platform step; `init --force` on non-JS repos; hints pointing at the `.gemini/helpers` duplicate; `[SHARED_INSTRUCTIONS_OVERLIMIT]` on every start; init's garbled closing sentence; `mcp status` saying `Stopped` right after `mcp verify` passes.

---

## CLI Commands: Diagnostics, Security, Hooks & Ops (part of `@monoes/monomindcli`)

The command dispatcher and parser (`index.ts`, `parser.ts`, `suggest.ts`) and the operational, diagnostic, and security command surface: `doctor`, `security`, `tokens`, `performance`, `hooks`, `memory`, `config`/`providers`, `status`, `guidance`, `completions`, the generated bash/write gates, the crash reporter, and `input-guards.ts`.

### Achieved Milestones
- Measured startup: `--version` 60 ms, `status --json` 226 ms, `status` 238–255 ms, `--help` 322 ms; `memory search` 149–193 ms wall of which the search itself is 2 ms (3,258 entries, 33.5 MB FTS5/WAL DB), `doc search` 146–200 ms (1,782 docs) — ≥ 95 % of both is CLI boot. `hooks worker run`: map 245–367 ms, ddd 188, audit 319, consolidate 150, security 236–246 (walks 2,571 files). MCP server (stdio) RSS: 69 MB after `initialize` (327 ms from spawn) → 89 MB after `tools/list` → 108 MB after `monograph_query` → 166 MB after `knowledge_search` (+58 MB), then flat — acceptable, no item. Non-TTY hygiene: ANSI = 0 bytes; `NO_COLOR`/`--no-color`/`FORCE_COLOR=0` honored; `[INFO]` to stderr; top-level typos get did-you-mean (top level only).
- `doctor`: 28 health checks via a `componentMap` dispatch table with `--fix` for helper files, gitignore, sidecars, shims; prints API-key *names* only.
- `security scan` (explicit `ScanCoverage`, SARIF), `security cve`, `security audit`, `security defend`, `security redteam --target` (live). Generated gates on `pre-bash`/`pre-write` (precision and fail-open paths: see Future). `input-guards.ts`: typed validation, symlink-aware path guard, URL allowlist.
- Inbound surfaces verified: dashboard 127.0.0.1 + Host allowlist + 0o600 token, CSRF posture sound (token same-origin only; `?token=` used only for `EventSource`, never in a navigated URL); org daemon Host + Origin + timing-safe credentials; MCP remote token-gated; state files 0o600.
- Token accounting (`token-tracker.cjs`, `tokens today|week|month|summary`): walks `subagents/`, dedups by `message.id`, prices cache read/write separately — internally consistent with the statusline *and* ~3× wrong (next section).
- Crash reporter: redacted title, ledger dedup, 5-per-repo circuit breaker, local fallback — and default-on public issues (below).
- **Fixed (2026-09-16):** `monomind status`'s System Resources table now renders instead of always printing "Resource governor not available". Root cause: `displayStatus()` assigned an unbound method reference (`output.warning`/`.error`/`.success`, prototype methods that read `this.colorEnabled` via `this.color()`) to `memColor` and called it later as `memColor(text)`, throwing `TypeError: Cannot read properties of undefined (reading 'color')` inside a bare `catch`. Now wrapped in closures (`(text) => output.warning(text)`, etc.); the catch also logs the underlying error via `output.printDebug` instead of swallowing it silently. Regression-tested end to end (`status-resource-governor-crash.test.ts`).

### Future Milestones
- **[9/10]** (quick win) Cost figures are understated ~3× with no "unpriced" signal: `claude-fable-5-1` (502 of 971 calls in one day) has no row in any of the **four hand-synced pricing tables** (`pricing/model-pricing.ts`, `ui/collector.mjs:248`, `ui/server.mjs:63`, `token-tracker.cjs:16`) → $0; `claude-sonnet-5` priced at Sonnet 4.6 rates; repo $36.26 vs ≈ $111.8 at published rates; 1 h-cache writes lumped with 5 m; the parity test `model-pricing.ts:24` promises doesn't exist. One pricing source, fable-5/5.1 rows, fix sonnet-5, the parity test, `unpriced`/`costIncomplete` rendered as `≥$36 today`, a `doctor` check that every model in 30 days of transcripts has a row. `[BUDGET_BREACHED]` (auto-tuned from the understated number, advice muted) follows.
- **[9/10]** (quick win) The crash reporter is default-on (`crash-reporter.ts:109` `{ enabled: true }`) and files **public GitHub issues under the user's identity** (`:275-330`: the user's `gh` auth, then `GITHUB_TOKEN`, to `gh issue create` on public `monoes/monomind` with a stack trace containing file paths). Prompt once (TTY only; non-TTY → local save), print the local report path, show the state in `doctor`/`init`.
- **[8/10]** (quick win) Unknown subcommands and flags succeed silently: `memory lisst`, `agent bogus`, `hooks bogus`, `session bogus`, `doc bogus`, `security bogus`, `tokens bogus`, `monograph bogus`/`monograph query` all print group help to stdout and exit 0 (only `org` exits 1) because the `findChild` walk (`index.ts:228-238`) stops at the first unmatched segment and runs the group action; `status --bogus-flag` runs normally because the global parser is built with `allowUnknownFlags: true` (`parser.ts:757`). `formatSuggestion(…, 'subcommand')` exists (`suggest.ts:108`) with zero callers; `COMMON_TYPOS` maps `doc → doctor` and `stats → stats`. Reject leftover positionals with a suggestion on stderr and exit 2; flip `allowUnknownFlags` to false with per-command opt-in; a dispatch test over every group.
- **[7/10]** (quick win) Destructive-ops gate precision and message: `gates-handler.cjs:226-239` regexes match literal text anywhere (a grep for a SQL DELETE, `git log --grep=` with "drop table", an `echo` about recursive deletes, `--force-with-lease` all exit 2) while `find ./dist -delete` and `git branch -D main` pass; the block reason (`:344`) wrongly says no confirm path exists (`permissionDecision: "ask"` does). Tokenize, `ask` for destructive, `deny` only for the unrecoverable set.
- **[7/10]** (quick win) Pre-write gate fails open on four paths (3 s settings timeout vs 5 s internal timer; > 1 MiB stdin silently `''`; stdin > 500 ms after spawn → allow; content sliced at 512 KiB). Gate in its own hook entry, no stdin timer for gates, `ask` on truncation, chunked scan.
- **[7/10]** Error messages don't say what to do next, and several are doubled or mis-levelled (`org status nonexistent` on stdout *and* `[ERROR]` on stderr; `memory retrieve` failing as `[WARN]`; the README's own `doc search "…"`/`memory search "…"` dropping the positional and saying `Required option missing: --query` while `route`/`search` accept positionals; `security cve BOGUS-1` printing usage with exit 0). Actions return `{message, hint}`; the sole required string option accepts the first positional; a CI test that runs every fenced `monomind …` snippet in README/doc with `--help`.
- **[6/10]** JSON output is a lottery (`--json` ×26, `--format` ×14, `--output` ×16, `--out`; `--json` ignored where `--format json` works; 11 commands honor neither; `platforms plan --json` exits 1 with zero bytes on both streams; no leaf `--help` documents it). One `ctx.format` in the dispatcher and a contract test that every `list|status|stats|summary|plan|tools` subcommand emits pure JSON (subsumes `doctor --json` and the six-key help-string drift).
- **[6/10]** (quick win) `analyze diff --file /nonexistent.ts` prints a partial box then `TypeError: Cannot read properties of undefined (reading 'length')` (`analyze-diff.ts:191-235`). Guard it. (The same-family `monomind status` System Resources crash is fixed — see Achieved.) And `memory search --mode keyword` is silently accepted and ignored (prints "requested: semantic"; the real flag is `--type`) — a data point for the unknown-flags item.
- **[5/10]** (quick win) Nine flags declared, documented, and never read (`route -k/--keyword` declared twice; `monoswarm --parallel`; `hooks --auto-configure`/`--restore-context`; `design palette --random`; `analyze --embedding-device`; `browse --account`; `config init --v1` — the help's own example; `org events --ndjson`); `help`/`version` aren't commands, `-v` alone prints `[DEBUG]`, `--version` appends `  ✓ up to date`; unify secret detection (`security scan`'s quote-only patterns vs the gate's richer list; #217); `doctor` process-liveness checks and `control-stop.cjs --session` on SessionEnd; `doctor` shelling `npm view` every run and non-actionable warnings; wire `security defend`/`redteam` into the audit trail; `MONOMIND_MONOFENCE_GATE=required`.
- **[4/10]** No exit-code taxonomy (293× `exitCode: 1`); non-TTY noise (blank leaders, `status`'s 3 `[INFO]` lines, `memory search`'s unrequested semantic-fallback warnings, `$0.00000`); flag synonyms; bounded state (`build.log`/watch log never rotate, backups never prune, the 256 KiB `routing-feedback.jsonl` read guard) and hook RMW counters without the P2-21 lock (not reproduced at 40 parallel hooks); per-session state isolation.
- **[3/10]** (quick win) `--help` loads every command implementation and runs the update banner.

---

## Dashboard (Neural Control Room) (part of `@monoes/monomindcli`)

`monomind ui` (alias `dashboard`): a local web UI over projects, sessions, tokens, memory, orgs, monograph, documents, and human-input queues — ~28k lines plus an 818 KB / 14,134-line `dashboard.html`, auto-started on every SessionStart by `control-start.cjs`. It has by far the most fix commits of any surface (200, 167 of them in June 2026).

### Achieved Milestones
- ~122 API routes (32 mutating), 3 SSE streams, 14 `fs.watch` sites (500 ms debounce; SSE clients removed on `close`). Auth verified live: default-deny for all `/api/*`, timing-safe compare, Host-header loopback allowlist, CORS reflects only its own origin, 0o600 token file, validated org names, per-process token rotation (a stale token cannot authorize a new server). OAuth to monoes.me uses a random `state`, PKCE S256, 10-min TTL, a fixed loopback `redirect_uri`.
- 12 sidebar views plus two standalone unlinked pages. 14 test files (~1,780 lines) — security, memory-browser API, shell-injection, cost-honesty rendering, control-start, 2 regex-presence tests on `dashboard.html`.
- Measured (fresh `ui --no-open` process): time-to-first-200 on `/` 484–523 ms; RSS 234 MB at first response, 281 MB after the sql.js run-event index initialises (`server.mjs:339`, +49 MB), flat afterwards; idle CPU 0.4 %; 26 fds, one inotify fd. Cheap routes are cheap: `/api/status` 1 ms (8.7 KB), `/api/projects`, `/api/orgs`, `/api/memory/stats` 0–1 ms, `/api/search-sessions` and `/api/session-errors` 10 ms; 100 concurrent `/` requests complete in 70–81 ms wall. `control-start.cjs` returns in 31 ms and does not wait for the server (confirmation is a detached child).

### Future Milestones
- **[9/10]** (quick win) 113 dashboard controls throw `ReferenceError` — commit 9537930fb prefixed 129 function declarations with `_` and the inline `onclick="x(...)"` strings were never updated; verified live (`typeof switchMemTab → "undefined"`, `_switchMemTab → "function"`). Dead: all Memory and Monograph sub-tabs, Human Input "Answer", loop stop/create, memory/chunk/doc edit+delete, monograph rebuild/watch/export, session bookmark/export/notes, budget modal, org config save, the playbook builder, ~40 toggles — in the published 2.10.30. One `Object.assign(window, {…})` block; a test that every `on*="name("` has a global definition; a UI ↔ route contract test (`/api/monograph-wiki-search` 404, `/api/data` → `base is not defined`, `DELETE /api/memory/entry` empty body → 500; ~14 routes with no caller) as the guard.
- **[8/10]** (quick win) The monoes.me OAuth access token is written into the project's `.mcp.json`: `routes-monoes.mjs:93-119` `_syncMonoesMcpEntry` sets `mcpServers.monoes.headers` to a literal `Bearer` header carrying the access token, rewritten on every dashboard load (`:261-269`); `.mcp.json` is created by init (`write-claude.ts:250-259`) as the shared project MCP config — the file Claude Code expects teams to commit; only this repo's own `.gitignore` excludes it; the refresh token sits next to it in `monoes-connection.json` under the *project* dir. Keep tokens in `~/.monomind/monoes-connection.json`; reference via env expansion or an `mcp-remote`-style helper; never write the literal.
- **[7/10]** (quick win) The Tokens and Sessions views overcount 2–2.5× (no `message.id` dedup in `collector.mjs:381`/`server.mjs:2146`), miss 46% of calls (`subagents/` skipped), and re-parse every transcript on every request: `collectTokens()` (`collector.mjs:311`) re-lists and re-parses the project's session JSONLs per call (the only "cache" is the `token-summary.json` header read), so `/api/project-costs` and `/api/token-usage` measure ~90 ms each with **142 ms CPU per request**, and one pass of the Tokens routes grows RSS from 282 to 479 MB, not reclaimed. One shared `iterAssistantMessages()` with an mtime-keyed per-file cache of parsed rows — the dedup fix and the cache are the same pass; `costIncomplete`; fixture test.
- **[7/10]** Duplicate dashboards and a hardcoded port (`npx` fallback + auto-increment → multiple live instances; `dashboard.html:11435-11851` hardcodes `localhost:4243` for the monobrowse builder; `control-start.cjs` re-broken 4× in a week). Measured on one developer machine: **four dashboards live at once** (4242 at 288 MB after 15 h; 4243/4244/4245 are `~/.npm/_npx/…/monomind ui` fallbacks auto-incremented from three other projects at 145/427/174 MB) — **≈ 1.03 GB RSS for one user**, each holding its own sql.js WASM index and inotify watcher. One machine-wide dashboard, bound port in `control.json`, 4243 from `active-port.json`, `ensureDashboard()` state machine with a per-OS test, the sql.js index in one process only.
- **[5/10]** (quick win) `/api/monograph` shells out to the `sqlite3` binary three or more times per request via `execFileSync` against the 191 MB graph DB (`routes-monograph.mjs:1523-1560`): 84 ms per request, 2.5 s wall for 30 calls with only 0.41 s CPU, and `[]` on machines without a `sqlite3` binary — while the same server already loads better-sqlite3 elsewhere. Use the existing DB handle; cache counts keyed on `index_meta.indexed_at`.
- **[4/10]** `/api/monograph-benchmark` (`routes-monograph.mjs:1472-1510`) always fails: it shells out to a command literally named `graphify benchmark <path>` — no such binary is installed anywhere in this repo or its dependency tree, so `execSync` throws (caught, surfaced as a 500 with the raw error text) on every call. Its `graphify-out/graph.json` legacy-path fallback (checked before this line, `:1479`) is presumably a genuine leftover from the product's own pre-"Monograph" era and can stay — but the benchmark shell-out itself either needs a real local implementation or should be deleted along with its dashboard button.
- **[6/10]** Test the biggest surface for real (`routes-org.mjs` 2 of 64 routes tested; `dashboard.html` zero executed tests; the June burst clusters into SSE dedup, handler wiring, field-name drift, org-switch races, XSS/token attach). The approve and answer routes bypass or under-authenticate the daemon (see Org Runtime fidelity (c)).
- **[5/10]** (quick win) The server writes into users' repos outside `.monomind` (`data/known-projects.json`, `data/unknown-events.jsonl`, `data/sessions/_index.json`, `.claude/scheduled_tasks.lock`) and `run-events.db` lives under the project, not `~`. Relocate.
- **[4/10]** Knowledge live-ingest watcher has no concurrency cap (`server.mjs:1647`; inferred to chain into the measured SQLite failure); `?token=` accepted on non-GET (hygiene; add a `Sec-Fetch-Site` check); `gsap@3` major-only CDN pin; 221 empty `catch {}`.

---

## Documentation

`README.md`, `doc/` (deployed raw to GitHub Pages by `pages.yml`, 17 concept docs), `docs/platforms/`, `doc/llms.txt`, and the generated `CLAUDE.md`/`GEMINI.md`/`AGENTS.md` templates. Round 3 executed every documented command snippet, checked every link, and diffed env vars and tool names against code. Round 5 read the README's 478 lines as a prospective user and graded ~45 promises against code or a live probe.

### Achieved Milestones
- The README quickstart works end to end in a scratch project when the `memory search`/`doc search` snippets are run with `-q` (as written they fail — see below); 158 of 160 documented command names exist; all 28 `hooks` and 13 `org` subcommands in the docs are real; the last 5 CHANGELOG entries are accurate and candid; `docs/platforms/compatibility.md` is generated from the registry.
- Promise audit verdict: not a different product. Orgs, Monograph, Second Brain, memory, and the five-platform init are real and mostly do what the README says; verified true as written: opt-in routing model never silent in CI (`init.ts:529-558`), Second Brain self-activates on `init`, `sample-team.json` is written and runnable, `org status` detects crashed daemons, crashed sessions restart with backoff (`daemon.ts:389`), 9 workers refreshed after 6 h, no post-install download scripts, the global brain's `[global]` label. The gap has two shapes — a retired command generation still documented, and precise-sounding claims with no code behind them — plus omitted caveats. **Under-sold:** `org` has 35 subcommands (`events`, `watch`, `inbox` over SSH, `approvals`, `replay`/`branch`/`resume-from`) and 14 runtime backends where the README names two; `doc reconcile/import/remove/eval` (Recall@k/MRR scoreboard), `security redteam --target`, SARIF output, the real audit trail, monobrowse's 60+ subcommands, the dashboard's 12 views, `tokens today|week|month`, `doctor`'s 28 checks with `--fix`, `analyze diff`, and shell `completions` are absent or one row.

### Future Milestones
- **[9/10]** (quick win) The Quickstart's first command doesn't exist. README :200-234 ("Autonomous Build Loop"), :261, :382-387, :397 tell a new user to run `/mastermind:autodev` (with `--tillend`, `--focus`, `--maxruns 50`), `/mastermind:build`, `tdd`, `architect`, `approvev1`, and `runorgv1` "by that explicit legacy name" — none has a file in the root or packaged `commands/mastermind/` tree (`debug`, `review`, `plan`, `worktree`, `improve`, `loops` do); only `master.md:259` still advertises `autodev`. Restore `autodev.md` or retarget the six references (`/mastermind:improve --tillend` / `loops` is the live equivalent); a CI test that every `/mastermind:<x>` in README/doc resolves to `.claude/commands/mastermind/<x>.md`.
- **[7/10]** (quick win) Invented specificity — delete rather than soften. README :297-310's L0→L3 memory-tier diagram with "BM25 K1=1.5, B=0.75" and "Federated swarm reads": no tier, promotion, or K1 constant exists anywhere in `@monoes/memory` (:36 "JSON pattern store with episodic recall" contradicts :295's own SQLite+vectors description). :285 "golden-set eval runs in CI with an 80% recall bar" — the test is `rate >= (semantic ? 0.75 : 0.25)` (`cognee-port-eval.test.mjs:102`) and CI has no model, so the floor is 25%. :272 "~60 ms per lookup" — unmeasured, and the injection it describes is off by default (`route-handler.cjs:124-129` under `MONOMIND_HOOK_QUIET=1`; the Init hook-enrichment item). :101 "Claude Code … prompts you before executing anything sensitive" — init-installed hooks and gates run on every tool call without a prompt, and gates fail open on four paths. Replace the memory section with one true sentence (one SQLite store, local-embedding cosine, size-gated HNSW above 5k, keyword fallback when no model is cached); state the real eval floor; reword :272 as "available by setting `MONOMIND_HOOK_QUIET=0`"; replace :101 with what runs automatically.
- **[7/10]** The deployed docs site can't reach the docs: `doc/index.html` is a 12-page SPA with zero links to any `.md`, so the 17 concept docs, `getting-started.md`, `llms.txt`, and `doc/commands/` deploy as plain text nobody can navigate to; no TOC anywhere; README links 5 of 17 concept docs; 274 of 521 links across 64 doc files are broken — 263 written repo-root-relative from inside `doc/` (404 on GitHub and Pages), 8 truly missing; `packages/@monomind/cli/README.md` is a byte-identical copy of the root README whose `doc/` links break on npm. Fix the 263 links (quick win), a Docs page or `doc/README.md` TOC linked from `index.html` and README, the link checker in CI, a 20-line pointer instead of the README copy.
- **[7/10]** (quick win) README snippets fail inside an initialised project: `memory search "auth"` drops the positional (CLI section), and the monofence example at :364 — `isSafe(…)` called with the README's own instruction-override sample string — trips the generated pre-bash gate at 95% confidence when pasted (reproduced during the audit; the gate blocked the reviewer's probe). The snippet-runner CI test plus the code-literal/meta-mention discount in the gate (monofence-ai).
- **[7/10]** (quick win) Fix the privacy and trust claims everywhere they're made: `package.json:4` "no data leaves your machine" (and names 2 of 5 platforms), README:99 "never to a monoes-controlled server" (the egress list at :99 also omits "Upload org" and `doctor`'s `npm view`), and three "the only outbound request monomind ever makes" sentences (`doc/llms.txt:8` — the file other LLMs read first — `getting-started.md:112`, README:280) all contradict the audited egress (npm on every run, `doctor`'s `npm view`, default-on public crash issues, the opt-in monoes.me upload) and the at-rest findings. One canonical "What runs automatically / what leaves the machine / what's stored where" table in README; everything else links to it; regenerate the `package.json` description; `monomind doctor --network`; add `mcp` and `claude-code` GitHub topics.
- **[6/10]** No environment-variable reference: 61 `MONOMIND_*` vars are read in code, 28 documented, intersection 17 — 44 read-but-undocumented (`MONOMIND_HOOK_QUIET`, `MONOMIND_SDK_AGENT`, `MONOMIND_CRASH_REPORTING`, `MONOMIND_AUTO_UPDATE`, `MONOMIND_DATA_DIR`/`HOME`/`CWD`, `MONOMIND_MCP_FULL`, `MONOMIND_MCP_ALLOW_REMOTE`, `MONOMIND_ENABLE_TERMINAL`, `MONOMIND_ALLOW_BROWSER_EVAL`, the resource-governor trio, `MONOMIND_HNSW_THRESHOLD`, …) and 9 documented-but-inert; `hooks.md:281/285` name the wrong vars (`MONOMIND_DEBUG` → `DEBUG`; `MONOMIND_LOG_LEVEL`). A single `env.ts` registry all `process.env` reads go through, a generated `doc/commands/environment.md`, a lint that every `MONOMIND_*` read is registered.
- **[6/10]** (quick win) Tool surface documented gate-blind: 155 of 251 real tools appear in no doc; 57 documented tools are hidden from `tools/list` by default and the docs don't say so ("19 default monograph tools" — 6 advertised; "20 memory tools" — 8; "88 tools" in antigravity/opencode docs); 17 phantom names in `docs/mastermind/plans/*`. `monomind mcp tools --markdown` → `doc/commands/mcp-tools.md` with a visibility column; a `tools` counter in `generate-doc-counts.mjs`.
- **[5/10]** (quick win) Counts that disagree between README lines ("30+"/"49" Mastermind commands vs 44 files; "28" vs "29 hook subcommands"; "31" org subcommands vs 35 live; "32 commands" vs 33; "19 default MCP tools" vs 6) — extend `generate-doc-counts.mjs` markers to the org-subcommand and mastermind-command counts; `doc/llms.txt` wrong on every count and stale on privacy; ten concept docs pinned 2.8.3–2.9.0, `memory.md` "v3.0.0" vs 1.0.17, `monograph.md` v1.5.6; `mcp-server.md` v1.0.1 and a nonexistent `org-tools.ts`; `org-runtime.md` §4.5 stale line refs and no replay/branch/resume-from section; `monoswarm.md` describing a different product; `hooks.md:27` "silently"; the "one true HNSW story" (`memory.md:118/135` "tries `HNSWIndex` first via `getHNSWIndex()`" — that function no longer exists; the CLI's `hooks intelligence --enable-hnsw` help says "dead fallback path"; `doctor` says "HNSW search enabled"; the truth is the size-gated ANN inside `SqlBackend.search()`); `getting-started.md:102` cites a nonexistent `monograph status`; `memory.md:172-180` uses `-v` as a value flag and a nonexistent `memory import --format`. Generate `llms.txt` and the counts; drop per-doc version headers; run the snippet check in CI.
- **[3/10]** (quick win) CHANGELOG headings missing 2.10.21–2.10.26 (2.10.28's text cites fixes "from 2.10.25/26"); have `check-publish-versions.mjs` require a heading for the version.

---

## @monoes/hooks (`@monoes/hooks` v1.0.6)

A standalone library — not the live hook dispatcher. Hook type/event definitions, an in-memory `HookRegistry`/`HookExecutor` (no runtime consumer in monomind), and a `WorkerManager` with 9 on-demand background workers (staleness-gated, hard timeouts, atomic writes) whose output feeds the statusline, router, and `doctor`. Tests cover 7 of 9 workers.

### Future Milestones
- **[4/10]** Concurrent-session safety (`workers-state.json` last-writer-wins; two sessions double-run stale workers); workers are awaited inline at session start (528 ms when stale; ddd every session — the canonical fix is the `session-restore` item under Init); worker-level tests for `progress` and `reflexion`.
- **[3/10]** `HookRegistry`/`HookExecutor` have no runtime consumer — label "library-only" or delete; drop the empty `WORKER_ALIAS_MAP`; `package.json:4` still says "8 workers".

---

## @monoes/memory (`@monoes/memory` v1.0.17)

Standalone memory backend underneath the CLI — a unified `IMemoryBackend` over pluggable SQLite drivers (native `better-sqlite3` primary, `sql.js` fallback), a pure-JS `HNSWIndex` (this package's — the *CLI's* separate standalone fallback index is what was removed), the document chunker used by the Second Brain, and cross-session checkpointing. Real dense embeddings are generated in the CLI and injected.

### Achieved Milestones
- Unified `SqlBackend` over a `SqlDriver` abstraction; WAL + `busy_timeout` (present, but ineffective for writes — next section); size-gated HNSW inside `SqlBackend.search()` (threshold 5000, persisted next to the DB); `better-sqlite3 ^12` mandatory; LanceDB gone (the directory name survives). ~13–14 test files (+ 46 skipped in two better-sqlite3-only files); alpha-tagged with an export guard. Content-hash idempotent ingest verified for a single writer; fences respected by the chunker.

### Future Milestones
- **[8/10]** (quick win) Every write transaction is deferred, so cross-process writers fail with "database is locked" despite `busy_timeout`. Measured: 6 parallel `memory store` writers × 5 → **6 of 30 failed**; 24 parallel → **9 of 24**; `doc ingest` under contention reported `partial store: 1/2 chunks — version not committed` for two files with exit 0. A standalone harness (8 procs × 25 read-then-write transactions, WAL, `busy_timeout=5000`) pins the cause: better-sqlite3's default deferred `BEGIN` → 25/200 failures (`SQLITE_BUSY` 15, `SQLITE_BUSY_SNAPSHOT` 10); `tx.immediate()` → 0/200 — `busy_timeout` never retries a read→write upgrade on a stale snapshot. `sql-driver.ts:125` and the sql.js emulation at `:241` are both deferred; every write in `sql-backend.ts` and the schema init on every open go through them. Real-world concurrency exists today (MCP server, hook subprocesses, the dashboard's live-ingest watcher, `org_remember`), and `memory-bridge.ts:25` documents ~12 `catch { return null }` sites that swallow exactly this error. `.immediate()` / `BEGIN IMMEDIATE`, a bounded retry on `SQLITE_BUSY*`, make `partial store` non-zero, a two-process contention test.
- **[8/10]** The #223/#224 "coincidental match" fix over-filters natural-language questions in keyword mode: the coverage gate (`memory-bridge.ts:1121-1125`, `matchedFraction >= 0.3`) counts stopwords, so `"how do we handle tokens"` → 0 results while `threshold: 0` returns the right entry at 1.0; the cognee fixture question returns nothing. Drop stopwords/short words before computing coverage; a keyword-mode assertion with a real floor (the current `test:semantic-eval` passes with a 1-of-4 floor).
- **[6/10]** HNSW threshold and invalidation make the ANN path a regression at CLI scale (6.5 s build at n=5k for a 0.9× query; 50 s at 20k; full rebuild on every `store()` above threshold; 11–44 MB JSON). Raise the threshold, incremental insert/tombstone, binary persistence; cite measured numbers or delete `PERFORMANCE_TARGETS`.
- **[6/10]** Chunker cuts mid-word and mid-table on the overlap side (measured on `memory.md`); snap `startChar` to the previous newline outside a fence, never cut inside a table. Embedding model cache inside `node_modules/@huggingface/transformers/.cache` (re-downloaded per npx hash; no `HF_HUB_OFFLINE`) — `~/.monomind/models`, the prerequisite for the silent-ingest fix.
- **[5/10]** (quick win) Advertised-but-missing normalizations (`embeddings_status` lists L1/minmax/zscore; only L2 exists; Poincaré is real); 46 permanently-skipped tests (`describe.skip` → `skipIf`); 20 atomic writers repo-wide share a fixed `${file}.tmp` (`memory-migrations.ts:84` exports the whole sql.js DB through one) — route through the pid-named `writeFileAtomic` (`cli/src/utils/json-file.ts:82`) and lint the literal.

---

## @monoes/monograph (`@monoes/monograph` v1.6.4)

Monomind's in-process, SQLite-backed codebase knowledge graph engine — tree-sitter WASM parsing, symbol/relationship extraction into SQLite, derived analyses, git-based freshness; a standalone LSP server. Round 5 asked the MCP tools 8 realistic questions as an agent would and judged them against grep/Read ground truth: the graph won 2 (`importedBy` of `input-guards.ts`: precision 100%, recall 6/7; callers of `atomicWriteFile`: 14/14, cleaner than grep), tied 1, lost 5 — and twice answered confidently and wrongly (bare `decide` resolved to a test-file method with blast radius 0; `writeFileAtomic` resolved to the `.agents/skills/monodesign` mirror copy). Every call returned in milliseconds; the problem is answer quality, not speed.

### Achieved Milestones
- 14 bundled tree-sitter WASM grammars (`tree-sitter-typescript@0.23.2` is the latest release) plus a regex fallback for 5 languages; measured parse-recovery loss is zero across 1,669 files; CALLS-edge precision ~100%.
- 18-phase pipeline with an `ExtractionCache`, one SQLite transaction per build, WAL + `busy_timeout=10000` — concurrent MCP reads during a forced rebuild verified clean; 5-state freshness model; FTS5; 46,431 nodes / 76,015 edges here, warm rebuild ≈ 8 s.
- Blast-radius analysis, Louvain + Leiden, 46 MCP tools (19 default / 27 advanced — only 6 reach the default `tools/list`), chokidar watcher, standalone LSP server. Published tarball 29.6 MB unpacked, 26.1 MB of it wasm.

### Future Milestones
- **[9/10]** Native-module install resilience and the Node floor — one decision. Open #231 (5 comments, unresolved): `prebuild-install` serves a wrong-ABI better-sqlite3 binary that survives rebuild/reinstall on Node 26; related #219, #244 (doctor reports a resolved native failure as current), #134/#232/#237 (a stale global monograph shadows the pinned dep). No graph means no `monograph_query` — the core promise. Meanwhile every package declares `engines.node >=20` (or `>=18`, or nothing); Node 20 reached EOL on 2026-04-30; `ai@7`, all 11 `@ai-sdk/*` providers, and `puppeteer` require `>=22`, so with `engine-strict` (root `.npmrc`) a Node 20 user silently gets no Vercel providers; `tests.yml` runs Node 22 only while `publish-smoke-test.yml` still tests `['20','26']`; better-sqlite3 13 requires ≥ 22. Ship `monograph doctor --native` (ABI expected/actual, prebuild URL tried, `--build-from-source` offer), degrade to a WASM sqlite fallback (sql.js is already a dependency of `@monoes/memory`), surface `build.log` errors, set `>=22.12` in all 10 packages, switch the smoke matrix to `['22','26']`, then take better-sqlite3 13 / commander 15 / chokidar 5 / vitest 5.
- **[8/10]** Resolve calls through dynamic imports, barrel re-exports, and nested closures (`scope-resolution.ts:180-235,373-376`; measured 38% caller recall, 0 cross-package CALLS edges despite 606 `await import(` sites). Round 5 added a same-package miss the barrel story doesn't explain: `buildAsync` has 7 production caller files by grep (`commands/monograph.ts`, `build-tools.ts`, `shared.ts`, `watcher.ts`, `web/api.ts`, `analyze-api.ts`, `executor.ts`) and the graph has 1 (14% recall); `watcher.ts:58` is a plain `await buildAsync(...)` inside `setTimeout(async () => {…})` with no dynamic import, yet identical lambdas in test files do get edges. Record dynamic-import bindings, follow RE_EXPORTS, a regression fixture for callee attribution inside arrow functions passed as arguments, resolution counters in `build.log`, measure recall on `buildAsync`/`decide`/`atomicWriteFile`.
- **[8/10]** (quick win) `monograph_suggest task=` is dead for code tasks. `mcp-tools/monograph/query-tools.ts:270-300` uses the task's hits only to filter edges with `confidence IN ('AMBIGUOUS','INFERRED')` — essentially doc→code REFERENCES — so "add a Windows branch to the hook generator" returns "No suggestions for this task. Run monograph_build first." on a fresh index (`_staleness: fresh`), and the only tasks that get output are ones naming a symbol a `.md` mentions, answered with "Why does milestone references kgSearch?" rather than a task question. Root CLAUDE.md mandates this tool before every 3+ file task. When no inferred edge touches the hits, derive questions from the hits themselves (top symbols, caller/callee counts, `importedBy`); say "run monograph_build" only when `nodes` is empty.
- **[8/10]** (quick win) Test lambdas drown production callers in every listing: `PolicyEngine.decide` shows 20 of 51 callers, all tests, with the 4 production callers (`session.ts:132/:606`, `runOneSession`, `test-loop.ts:91`) hidden in "… 31 more"; `buildAsync` 34 of 36 test; `PolicyEngine` inbound 50 of 60; `getProjectRoot` 9 of 20 are `() => {`. An agent reading the text concludes "only tests call this". In `context`/`impact`/`neighbors`, sort non-test paths first (`__tests__`, `.test.`, `tests/`), collapse tests to "+34 calls in 12 test files", keep `MAX_LISTED_CALLERS=20` (`impact-tools.ts:95`); `includeTests` flag; demote test paths in hybrid ranking too ("MCP tool that handles config_set" ranks `MCPTool*` interfaces and `t => t.name === 'config_set'` test lambdas above `[Tool] config_set`; "org runtime checkpoint write" never surfaces `daemon.ts`'s `writeJsonFileAtomic`).
- **[7/10]** (quick win) `context`/`impact` resolve an ambiguous name silently: `decide` → `fence.test.ts:169` (LOW / 0 symbols), `writeFileAtomic` → the skill-mirror copy (real one: `memory/src/atomic-file.ts`), with no "N other definitions" hint; `filePath: 'policy-engine'` → "No symbol found" with no candidates. `monograph_neighbors` already lists candidates on ambiguity — share its resolution, prefer non-test and non-mirror (`.claude/.agents/.gemini/.codex` prefixes) definitions, print the alternatives.
- **[7/10]** One build lock, one spawner path (three spawners + the watcher, two locks with different rules; builds > 5 min look stale) — read from code, no duplicate build reproduced; a spawner-coordination problem, not a DB one.
- **[6/10]** (quick win) Output defects that cost tokens or mislead: `Community: [object Object]` on every `monograph_context` result; `monograph_impact` appends a full JSON echo of the same data (~14 KB for `decide`, ~10× the text) — drop it or gate behind `format: 'json'`; `[Tool]` nodes carry `start_line 0` (`config-tools.ts:0`), so "jump to line" fails for the node type agents ask about most — record the object-literal line. No hub/degree query exists ("god nodes of `orgrt`" is unanswerable: `label=File` returns 7 test files, `label=Class` is name-ranked; fan-in by grep is `types` 98, `agent-runner` 18, `daemon` 14, `tool-fence` 12) — `sortBy: 'degree'` + `pathPrefix`.
- **[6/10]** (quick win) 82% of Document nodes are platform-mirror duplicates; `.monograph/` and `*.wasm` walked every build. Real incremental build (`buildIncrementalAsync()` is a full rebuild; first log per-phase ms, cache hit ratio, full parse-warning list, resolution counters). `churn-cache.ts:76` shells `git merge-base` with an unvalidated SHA from its own cache file → `execFileSync` + validation. Dead third-party cloud client hardcoding `https://api.fallow.cloud/v1` (no callers). `monograph_suggest`'s mislabelled "verify INFERRED" questions and 20% word-collision REFERENCES edges. DB bloat (27% free pages, 40 MB FTS).
- **[5/10]** (quick win) Type aliases stored as `Interface`; Svelte/Astro skipped; non-git `isStale: false`; wasm grammars as a release asset instead of 26 MB tracked.

---

## @monoes/routing (`@monoes/routing` v1.0.4)

Small, dependency-free package implementing the first two tiers of Monomind's task-to-agent routing cascade — a regex keyword pre-filter and cosine-similarity matching over route centroids — plus an LLM-fallback classifier. The CLI injects a real embedding model (`Snowflake/snowflake-arctic-embed-xs` in an isolated worker); this package never learns anything. 7 test files, 98 tests, 185 ms.

### Future Milestones
- **[7/10]** 38 of the 70 `agentSlug`s in `src/routes/*.route.ts` and `keyword-pre-filter.ts` have no agent file (the entire game-dev/product/design/marketing domains; `security-architect`; `tdd-london-swarm`); `route-layer.ts` never validates the slug; "audit security" → the nonexistent `security-architect`. Canonical fix: the reference-integrity lint under Init; here: a build-time test that every slug resolves, retarget the 38 (only 11 have a ≥ 0.75 near-match), fall back to `general-purpose`. Calibration note: this output reaches the model only through the muted hook channel today.
- **[5/10]** (quick win) Golden set (two negative cases exist; 12%/58% figures unreproducible); `hooks_route` silently uses the hash encoder when the worker is unavailable (return `method: 'hash-fallback'`); two divergent complexity heuristics; throw on a centroid/query dimension mismatch.

---

## @monoes/mcp (`@monoes/mcp` v1.0.4)

A self-contained MCP protocol engine (~7,800 lines; stdio/HTTP/WS transports, pooling, rate limiting, OAuth 2.1 client; each transport unit-tested). In practice HTTP/WS-only infrastructure: the CLI's primary stdio path uses neither this package nor the CLI's own hardened loop.

### Future Milestones
- **[5/10]** (quick win) Decide and document ownership (HTTP/WS-only) in `doc/concepts/mcp-server.md` and the README; migrate to express 5 / helmet 8 alongside monograph.
- **[4/10]** (quick win) `startHttpServer()` hardcodes `version: '3.0.0'` and never passes `connectionPool`; tool-registration failure is non-fatal; `corsOrigins: ['*']` default.

---

## @monoes/monobrowse (`@monoes/monobrowse` v1.0.8)

Monomind's native browser automation engine — a direct Chrome DevTools Protocol client, no Puppeteer/Playwright/Selenium. 60+ CLI subcommands, ref-based accessibility model, content-boundary sentinels, headless-to-headed redirection on login walls, 6 platform adapters, network interception/HAR, performance tooling, an embedded dashboard with a playbook Builder, documented three-layer crash recovery; cookie/session files under `~/.monomind/browser-sessions/` (0700). The CLI consumes it from npm at 1.0.6 instead of the workspace's 1.0.8.

### Achieved Milestones
- Round 4 ran the real flow through `node bin/cli.js browse -p 9333` against a local test page with Chromium installed: open, snapshot, find, get, eval, fill, click (form actually submitted), screenshot, pdf, a bad ref (`@e999` → clear error, exit 1), a second `open` on a live port (reuses the session and tab, no collision), and `close` — every step worked and no Chromium was orphaned (verified 3×). `browse --help`'s 63 subcommands match `doc/concepts/monobrowse.md` and both byte-identical `agent-browser-testing/SKILL.md` copies — no phantom commands; every skill flag tried is accepted.
- **Fixed (2026-09-16):** `openUrl()` (`browser.ts`) now inspects `Page.navigate`'s own CDP response and throws on `errorText` (e.g. `net::ERR_CONNECTION_REFUSED`) instead of always falling through to `waitForNetworkIdle` and reporting success once Chrome settles on `chrome-error://chromewebdata/`. `browse open` on a refused connection now exits 1 with the real error instead of 0. Regression-tested (`open-url-navigate-error.test.ts`).

### Future Milestones
- **[9/10]** (quick win) Every `browse` command idles ~5 s before exiting. `packages/@monomind/cli/bin/cli.js:372` sets `FORCE_EXIT_MS = 5000` (unref'd) and lets the loop drain (the onnxruntime rationale); monobrowse's CDP WebSocket and undici keep-alive (4,000 ms parser timers observed) keep the loop alive, so every connected command measures ~5.1 s (`get title` 5,132 ms, `fill` 5,100, `screenshot` 5,148) against **72 ms** for the same command through the package's own `monobrowse` CLI — a 20-step agent test burns ~100 s idle. Have `commands/browse.ts` dispose the CDP client (or exit) after a monobrowse subcommand, or make monobrowse's runner dispose its client; a test asserting `browse get title` < 500 ms.
- **[8/10]** `console`, `errors`, `har`, and `network requests` are unusable from the CLI, yet the skill builds its QA loop on them. Capture state is per-process (`har.ts:27` in-memory Map; `getConsoleMessages`/`getPageErrors` read nothing in a fresh process): `browse errors` prints "No page errors" on a page that `console.error`s and throws; `har start` succeeds and `har stop` fails with "No active HAR recording for this session"; `network requests` says "Run: network capture start" and `capture` is absent from `network --help`. `agent-browser-testing/SKILL.md:89,119,200-232,258` tells agents to run `browse errors` after every action and `har start/stop --bodies`. A capture sidecar started by `open` that spools `Runtime.consoleAPICalled`/`Log`/`Network` events to `.monomind/monobrowse/<port>/`, or document these as `batch`-only and rewrite the skill; add `capture` to `network --help`.
- **[7/10]** Flag position silently changes `snapshot` semantics and ref numbering: `browse -p 9333 snapshot` yields an interactive-only tree (e1 = textbox, no heading, so the skill's `find role heading` fails) while `browse snapshot -p 9333` yields the full tree (e1 = RootWebArea, heading = e2); `--help` lists `-p/-s` as top-level browse options, so the first form is the natural one. Fix the parser so leading global flags don't coerce booleans; test both orders; document that `-i` and full snapshots number refs differently.
- **[6/10]** Ship what the UI advertises: the Builder's `/api/playbooks*` calls 404 (`server.ts:109-147` routes none of them) and the printed `npx monomind browse playbook run <name>` subcommand exists nowhere. Add the routes and the command; agree with the main dashboard on the port.
- **[5/10]** State left behind: `/tmp/monomind-browser-<port>` profiles persist after `close` (121 MB each; 4 on this machine, one from monodesign's URL mode); screenshots and PDFs default to `/tmp/monomind-*`, never `.monomind/monobrowse/`; `browse` writes nothing under `.monomind/` or `~/.monomind/`; `~/.cache/puppeteer` holds 1.3 GB of two Chrome builds although system Chromium is installed; a 3-hour-old Chromium on port 9222 with 8 processes was already running when the lens started (live evidence for `browse doctor`). Default artifacts to `.monomind/monobrowse/artifacts/`, purge the profile on `close` (or `--keep-profile`), `browse doctor` lists stale profiles, orphaned Chromes (the 24 h PID-trust gap), and the puppeteer cache.
- **[5/10]** (quick win) Chrome discovery misses per-user Windows installs: `browser/types.ts:149-154` lists only `C:\Program Files*` while Chrome's default consumer install is `%LOCALAPPDATA%\Google\Chrome\Application\chrome.exe`, and the PATH fallback `browser.ts:56` is `which …` (POSIX) — "No supported browser found". Add `LOCALAPPDATA`/`PROGRAMFILES(X86)` candidates; `where.exe chrome msedge` on win32.
- **[4/10]** (quick win) `fill @e2` on a *button* reports "✓ Filled: button Submit"; the "Stale references — re-run snapshot" warning prints on non-ref commands (screenshot, pdf, har start, navigate reload). Validate fillable roles; warn only when a ref argument is given. A fixture suite for `detectAttentionNeeded` (zero tests); consolidate the CLI's diverging 4-adapter copy and duplicate dashboard; stop publishing `src/__tests__`.
- **Fixed (2026-09-16):** `close-browser.test.ts`'s "graceful close force-kills a process that never exits" case hit its hardcoded 20 s timeout three times this session, always as part of the full 16-file suite under heavy unrelated host load (passes in under 1 s run alone — confirmed each time, never a code regression). Raised to 60 s; the test's actual fake-timer work is trivial, so a genuine hang still fails well inside the new budget. Verified: a subsequent full `pnpm verify` run completed with zero failures anywhere.

---

## @monoes/monodesign (`@monoes/monodesign` v1.2.6)

Frontend design intelligence: 51 anti-pattern rules with severity, four-tier detection (regex → static HTML/CSS → live browser → visual contrast) scanning JSX/Vue/Svelte/Astro source directly, a monobrowse-first driver seam, `monodesign_fix` with a dry-run default, a 129-seed OKLCH palette, 50 test files. The CLI consumes it from npm at 1.2.2 (workspace: 1.2.6) and ships a byte-identical 2.5 MB copy of its `skill/`.

### Achieved Milestones
- Round 4 end-to-end: `design detect` on a scratch page → 148 ms, exit 2, two correct hits (low-contrast `#999`/`#fff`, marketing buzzword); the `jsx-should-flag.jsx` fixture → 7 hits in 89 ms, and the MCP `monodesign_detect` returns the same 7 findings in the same order (CLI/MCP parity); `fix --dry-run` explains every skipped finding and produces a real diff for `color.html`; `palette --from monomind` is deterministic (same seed twice) and emits only `oklch(...)`. URL targets do reach the browser tier (`-t http://…` → 1,025 ms, `small-touch-target` ×2 + low-contrast).

### Future Milestones
- **[7/10]** (quick win) `design detect --json` is not parseable and text findings go to stderr: `--json` stdout begins with the "Design Anti-Pattern Detection" banner (`JSON.parse` fails); in text mode 274 finding lines go to stderr and 5 to stdout, so `design detect | grep` finds nothing; exit code 2 on findings is undocumented. Banner → stderr, findings/JSON → stdout, document exit codes.
- **[6/10]** (quick win) The wrapper hides the detector's real surface and degrades silently: `monomind design detect` accepts only `-t/--json` while the engine has `--gpt/--gemini/--scope/--quiet/--no-config`, so provider-tell rules are unreachable (the `gpt-tells.html` fixture → 0 hits, no hint); file targets never reach the browser or visual tiers (no Chrome launch, sub-500 ms) and nothing says so; URL targets work but help says "HTML/CSS files"; browser mode drops the static findings for the same page (the buzzword finding vanishes). Pass unknown flags through, print which tiers ran, document URL mode, run the static rules on the rendered DOM too.
- **[6/10]** Low-contrast flood and cadence false positives on real code: 123 of `dashboard.html`'s 135 hits are the same three colour pairs (`#4f4d49`/`#343230` on `#060403`/`#15110e`/`#0e0a08`) repeated per element; `em-dash-overuse` reports "40 em-dashes in body text" for a file whose 307 em-dashes are mostly CSS/JS comments (`dashboard.html:142,436,1191`). Dedupe contrast findings by (fg, bg, file) with a count; strip `<style>`/`<script>` before cadence rules; add a fixture.
- **[5/10]** (quick win) Concurrent-launch test; partial results + `timeout_ms` on `monodesign_fix` timeout; drop the `puppeteer` optional fallback.

---

## monofence-ai (`monofence-ai` v1.0.2)

Monomind's standalone AI-manipulation defense package — a regex/heuristic engine that detects attempts to manipulate an LLM via crafted input (instruction overrides, alternate-persona framing, obfuscation/evasion, multi-turn escalation) and PII, and scans LLM outputs for leakage. Core `MonoDefence` API, 50+ patterns, evasion normalization, escalation machine, output scanning, opt-in learning, hook registration, CLI integration verified, ~1,230 lines of tests.

### Future Milestones
- **[7/10]** (quick win) Gate precision on documentation prose, and actionable block messages: the generated pre-write gate blocked this file at 99% because prose describing detection categories matches the ≥ 0.9-confidence instruction-override regex (`threat-detection-service.ts:40`); the pre-bash gate blocked reviewers' greps; #217. Meta-mention discount (backticks/code fences/regex literals, or a sentence whose subject is detection/pattern/category), a security-documentation BENIGN corpus, the matched snippet + escape hatch in the block reason.
- **[5/10]** The redteam library is a demo (20 prompts) — a versioned corpus; warn and require `--yes` when `--target` isn't loopback. Reuse input-side `api_key` regexes in `OutputScanner.PII_PATTERNS`; persist `ThreatLearningService`.

---

## Cross-Cutting Themes (scored)

1. **[9/10] Hooks must never break, slow, silently no-op, lie, or leak inside the host session.** Every session start pays ≈ 0.5–1 s, 80 % of it on a transcript walk, a muted graph query, and inline workers (the ~6 s subagent-spawn stall from `capture-handler.cjs`'s dangling timer is fixed — 2026-09-16); the pre-write gate fails open on four paths; the post-commit hook's `npx monomind@latest` hangs 70 s offline; the enrichment the hooks exist to provide never reaches the model; the statusline reports assists built on those unseen hints; and the same hooks write every tool input and prompt to a committable file unredacted. Every external bug report is a hook that broke a session.
2. **[9/10] Install and offline resilience.** Native-module ABI failures, an EOL Node floor contradicted by 15 deps, a root build that doesn't build the product, sibling packages consumed from npm, uninstallable tarballs, `@latest` resolution on every MCP start, knowledge features silently keyword-only when the model isn't cached.
3. **[9/10] Documented control not wired at runtime.** `PolicyEngine` only on the SDK runner; `buildIncrementalAsync` a full rebuild; `ConnectionPool` never instantiated; `browse playbook run` missing; four platforms install nothing; `platforms doctor` prints nothing; the fallback hook generator's stub; monoswarm's "MUST init"; `autopilot_*`'s Stop hook; `guidance_*`'s phantom tools; `learning-service.mjs`; 113 dead dashboard handlers; 38 phantom routing agents; 42 phantom MCP tools in shipped content; "resumable" orgs that lose worktrees; six runners that can't resume; `maxTurns` ignored by 12; a `doc eval` that can't run; a pricing-parity test that doesn't exist; the hardened MCP loop that never runs; `mcp toggle` that hides nothing; `listChanged` never emitted; nine flags declared but never read; `init --wizard` in the generated CLAUDE.md; 9 documented env vars nothing reads; a `commit-msg` hook that enforces nothing; `pnpm dev` that prints nothing; redaction that no hook calls; `/mastermind:autodev` and five sibling commands the README documents; `browse errors`/`har` that the shipped skill prescribes and that always return empty; `monograph_suggest task=` that CLAUDE.md mandates and that returns nothing for code tasks; a README memory architecture (tiers, BM25 constants, "80% CI bar") with no code behind it. An audit pass — for every documented capability, a test that reaches it.
4. **[9/10] Privacy: claims vs egress and at rest.** "No data leaves your machine" vs npm on every run, `doctor`'s `npm view`, default-on public crash issues, the monoes.me upload with inline secrets; and at rest: verbatim tool inputs/prompts at 0644, an OAuth token in a shareable `.mcp.json`, sensitive stores left committable by the generated `.gitignore`, the dashboard token fanned out into other repos.
5. **[9/10] Accounting honesty.** Cost understated ~3×, the dashboard overcounting 2.2× while missing subagents, `[BUDGET_BREACHED]` on the wrong numbers, the statusline discarding Claude Code's accurate `cost.total_cost_usd`, 8 of 14 runners with unreliable usage.
6. **[8/10] The CLI contract.** Unknown subcommands and flags succeed silently; JSON output honored on 4 of 27 probes; errors doubled, mis-levelled, or without a next step; positionals silently dropped by the commands the README demonstrates; a flat exit-code taxonomy; `analyze diff` crashing. One dispatcher fix plus a contract-test harness.
7. **[8/10] Agent-facing content and MCP roster: budget *and* correctness.** ~25k tokens of listings per prompt plus ≈7.5k tokens of `tools/list` per session (40% admin bookkeeping); 97 commands with no name/description; ~1,000 references to things that don't exist; 35 of 67 core tool descriptions are stubs; `.opencode/*` symlinked into `.claude/`; READMEs as commands; an empty capability registry; a generated CLAUDE.md that misinstructs.
8. **[8/10] Concurrency and write atomicity.** `@monoes/memory`'s deferred transactions fail under any write concurrency (measured) and the failures are swallowed; 225 write sites — 63 atomic, 24 safe JSONL appends, 138 plain; 20 atomic writers share a fixed `${file}.tmp`; the dashboard and daemon both own `approvals.json`; `init --force` copies live helpers non-atomically. Verified sound: monograph reads during a build, `Mailbox`, `respawnRole`, the broker registry.
9. **[7/10] Gate precision.** Monofence on documentation prose, the destructive-ops gate on quoted strings/comments, the graph gate on `ps aux | grep`/`Glob`, `classifyStderr` FATAL on any `401`/`403` substring — while real deletes, secrets past 512 KiB, and genuine timeouts pass.
10. **[7/10] Org runtime: 14 runners, one contract, and a federation hole.** Nine hand-copied `streamTurn`s disagree on timeouts and exit semantics; abort kills only the direct child; every child inherits the daemon's full environment; per-cwd session stores collide; six runners pass the prompt as argv; and SSH federation hands LLM output to a remote shell.
11. **[7/10] No quality gates.** Lint red with no lint/typecheck job; no coverage thresholds; no link, snippet, license, or size check; `test:all` ≠ CI; the primary MCP loop, the consensus code, `routes-org.mjs`, and `dashboard.html` have no executed tests; runner tests use fake children; the eval harness has never produced a number; the test suite leaks 29 dirs into `~/.monomind` per run; a `commit-msg` hook that can't run.
12. **[7/10] Documentation as a product.** The deployed site links to none of the docs; 274 of 521 links broken; no env-var reference; 155 tools in no doc and 57 hidden ones documented as visible; 7 copy-paste snippets that fail; `llms.txt` wrong on every count; no `CONTRIBUTING.md`.
13. **[6/10] Shell-string subprocess calls.** `remote.ts` (remote side), `churn-cache.ts:76`, `terminal_execute`, `github-tools.ts` pipes-as-argv → `execFileSync` with argv everywhere + lint rule.
14. **[6/10] Silent failure.** 280 empty `catch {}` in `packages/*/src`, 324 in shipped helpers; `SQLITE_BUSY` swallowed by ~12 bridge sites; JSONL runners dropping lines uncounted; `doc ingest` partial stores with exit 0; TTL/checksum refusals with the cause only under `DEBUG`; MCP `isError` lost in the shipping loops; a build script that silently skips the product.
15. **[6/10] Unconsolidated stores, data roots, and copies.** Monograph graph, memory KG, Memory Palace triples, monoswarm JSON under `.git/monomind` vs `.monomind/consensus`, four pricing tables, three statusline implementations, three MCP stdio loops, two adapter sets, two dashboards, a `lancedb/` directory holding SQLite, a monodesign skill shipped twice, express 4 and 5 side by side, a README duplicated into the CLI package, an orphan root `dist/`. Needs an ADR.
16. **[7/10] Windows / macOS coverage.** `cli`/`monograph`/`memory` excluded from Windows tests; `sh -c`-only hook wrappers; POSIX-only project-dir encoding. Round 5's static survey (every claim file:line; the mirror and tests excluded) found the codebase mostly portable — 0 `/proc` reads, 0 `'node'` literal spawns (`process.execPath` at 33 sites), `HOME || USERPROFILE` everywhere, 21 of 24 containment checks use `+ path.sep`, `wx` locks and tmp→rename writes are NTFS-safe, macOS `/private/var` handled — with a short list of real breaks: **(a) [7]** 18 of 21 `detached: true` spawns omit `windowsHide: true`, and four fire from hooks on every Edit/Write and statusline refresh (`edit-handler.cjs:112,120`, `utils/monograph.cjs:616`, `token-tracker.cjs:621`; also `executor.ts:422`, `helpers-generator.ts:595`, `forwarder.ts:96`, `routes-monograph.mjs` ×4, `monograph/mcp-tools/serve.ts:38`, `monobrowse/browser.ts:209`, `control-start.cjs:468,497`, `monograph-freshen.cjs:138`) — a console window flashes on every edit on Windows; one-line fix each plus a grep in the helpers-parity test (quick win). **(b) [7]** The control server and dashboard can never be stopped or reaped on Windows: `control-stop.cjs:26-36`, `cleanup.ts:502-515`, and `ui/server.mjs:324-334` gate `process.kill` on `ps -p <pid> -o command=` (the catch says "platform without ps — don't signal") while `control-start.cjs:40` trusts the pid as live — stale servers accumulate, `--session` stop is a no-op; one shared `describePid()` with a PowerShell/`tasklist` branch. **(c) [6]** Org auto-wake is silently dead on Windows: `ui/routes-org.mjs:1682` bare `spawn('npx', …)` throws `EINVAL` synchronously on win32 into a best-effort catch (`session-restore-handler.cjs:197-203` shows the fix) (quick win). **(d) [6]** `sh -c` leaks outside the generator: `statusline.cjs:182` (+ template `statusline-generator.ts:191`) runs four git commands through `sh -c` from Node → blank branch/dirty state on cmd.exe (use `execFileSync('git', …)`); `monograph/src/init/agent-hooks.ts:71` is a second, unshared hook-command generator writing `sh -c '… || true'` into settings.json that any generator fix would miss. Whether Claude Code on Windows runs hook commands via Git Bash or cmd.exe is not established anywhere in-repo — one verified sentence decides whether the generator needs a win32 branch at all. **(e) [5]** `browse-workflow.ts:122` opens the dashboard with macOS-only `open`; a correct `openUrl()` exists at `ui/server.mjs:835-844` but its `start "${url}"` treats the URL as the window title (`start "" "${url}"`) — export it, fix it, use it there and in `monograph/mcp-tools/serve.ts:37` (quick win). **(f) [4]** 33 `0o600/0o700` sites are a silent no-op on NTFS (only `memory/file-permissions.ts:17` is win32-aware; none verify the mode) — the "other local users can't read session tokens" comment at `monobrowse/session.ts:8-10` is false there; one sentence in the security docs, optional `icacls`. **(g) [3]** `monograph/reporting/graph-report.ts:409` defaults to `'/tmp'` (→ `os.tmpdir()`); `workspace-filter.ts:13`, `analysis-counts-snapshot.ts:32`, `specifier.ts:51` use `startsWith(root)` without `+ sep` (also case-insensitive-FS misses) (quick win).

---

## Pruned in this revision (and earlier)

Removed, corrected, re-scored, or verified-not-a-problem, so they don't get re-added:

- **Revision 8 (perf-2) — verified fine, no item:** MCP server steady-state RSS 166 MB; org bus append 24 µs/event and 52 MB bus reads in 125 ms; dashboard static serving under 100 concurrent requests (70–81 ms wall); `control-start` 31 ms; dashboard fd growth after a hammer is client keep-alive sockets, not a leak; `memory`/`doc search` inner search 2 ms (the wall time is the CLI-startup item). Observation recorded, not scored: the pre-write gate blocked a scratch script containing a `token=${…}` template literal — another false-positive data point for the secret/destructive gate item.
- **Revision 7 (late reports) — verified sound, so don't re-survey:** monobrowse open/snapshot/find/fill/click/screenshot/pdf/close all work, bad refs error cleanly, concurrent `open` on a live port is safe, no orphan Chromium after `close`; `browse --help` vs doc vs skill has no phantom commands; monodesign CLI/MCP parity, deterministic `palette --from`, oklch-only output, `fix --dry-run` reasons; graph latency (every call in ms — the issue is answer quality); cross-platform: no `/proc` reads, no `'node'` literal spawns, `HOME`/`USERPROFILE` handled, `.githooks/*` bash-3.2-clean, `wx` locks and tmp→rename NTFS-safe, `resource-governor.ts` and `hooks-coverage-commands.ts` already branch on win32, macOS `/private/var` handled at `ui/server.mjs:1189`; README promises verified true as written are listed under Documentation. Not re-added: "the README says `isSafe` runs in ~0.04 ms" (unverifiable here — `monofence-ai/dist` isn't built in this checkout).
- **Revision 6 corrections:** the capture-handler item's "every Bash/Write/Edit in every init'd project" reach was wrong — the generator wires it to SubagentStart/Stop only (per-tool-call entries exist in this repo's and the packaged `settings.json`); the crash reporter moved up (verified `{ enabled: true }` default); ~20 top-table items moved down for reach (org-runtime-only, docs-only, test-asks, dashboard-only); "the README quickstart works" and "the README's `memory search` snippet fails" are both true (`-q` works, the positional doesn't); `@monoes/memory`'s `HNSWIndex` is real — the removed index was the CLI's standalone fallback; "25 skipped memory tests" → 46 in two files; "~1,200 CLI tests" in `tests.yml` → 3,061.
- **Revision 5 verified sound:** MCP reads during `buildAsync` (measured no collision); hook RMW counters (no lost updates at 40 parallel hooks); `Mailbox`, `respawnRole`, the broker registry, `workers-state.json` writes.
- **Revision 5 corrections:** "`monograph query` shows help with exit 0" is dispatcher-wide, not monograph-specific; "`doctor --json` missing" folded into the JSON-contract item; "zero direct tests for `mcp-server.ts`" retargeted — that loop doesn't ship.
- **Revision 4 corrections:** "13 runner files" → 14; every runner has a dedicated (fake-child) test file; the eval harness has never produced a number; the registry is not polluted (that was the agent store); chunking is heading-aware on the cut side only; the #223/#224 fix is a coverage gate that over-filters.
- **Revision 3 corrections:** graph-gate state RMW is already atomic in the live helper; `.monomind/` is already ignored by the scanner; parse "errors" lose nothing.
- **Earlier prunes:** automated CHANGELOG generation; a declarative invariants runner; a "process for the behaviour suite"; "`org inbox` isn't registered" (it is); org-scoped KG namespaces (implemented); multi-hop `monograph_query` *search* ranking; "16 platforms have no init path"; "no user-facing support-level docs"; an OpenCode persistent statusbar; "three parallel memory backends"; a GEPA pipeline; timer scheduling in `@monoes/hooks`; asserting security hooks inside `HookExecutor`; "no on-disk ANN cache"; the toy hash embedder; "no JS-only coverage"; a bundled embedding model for routing; moving the outcome ledger into `@monoes/routing`; the `@monoes/mcp` external-adoption question; "the playbook Builder is UI-only"; "`browser_*` duplicates monobrowse"; framework-aware detection, severity weighting, and a palette hue audit in monodesign; full Unicode confusables in monofence.
